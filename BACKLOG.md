# GameNight OS Backlog

This file is the SOURCE OF TRUTH for project scope. The Excalidraw project map is a
rendering of it, nothing more: every zone on the map maps to one heading below, so the
map can always be redrawn from this file alone (see MAP PROTOCOL). If the two ever
disagree, this file wins.

Section order below is deliberate and matches the map's zone order. Do not rename or
reorder headings without updating MAP PROTOCOL in the same commit.

## MAP STATUS (update every session, no exceptions)
Read this FIRST, before any other work. The redraw rule is driven by this counter, not by
anyone's memory of how many sessions have happened.

    Last map redraw:                    2026-07-28 (third pass that day)
    Shipped sessions since that redraw: 2 (Smashdown; Smashdown series rows)
    Redraw due at:                      3

    Counter is below 3: continue with the requested work and increment it by 1 in the
    same commit as any session that ships. Reconciled + redrawn on 2026-07-28 (third
    pass that day) at the START of the Smashdown session, because the three sessions
    that shipped since the previous pass (phase 6, one TV button per night, one pack
    registry) all landed on 07-28 and handed over a counter of exactly 3.

    What this pass changed beyond the counter:
      - Zone 1 gained the two shipped sessions the map had never rendered, both marked
        (NEW): one TV button per night (/e/:id/tv auto-follows what is being played)
        and one pack registry (SESSION_PACKS in packages/shared/src/packs.ts). Phase 6
        needed no new item, since the whole cleanup run is deliberately ONE item, and
        that item lost its (NEW) highlight because it is now a redraw old.
      - NEXT UP renumbered. Slots 1-3 had all been open, since the cleanup run finished
        and James had not committed the next sessions. Smashdown is now slot 1 (this
        session), Smash Tournament format slot 2, Tabletop theme slot 3, in the pack
        priority order this file already carried.
      - Row 1 zones raised 800 -> 860 and row 2 pushed down 920 -> 980. Zone 1 had 92px
        of slack and the two new items need exactly 92px, which would have left a zone
        with zero slack for the next entry to overflow. Raised pre-emptively per MAP
        PROTOCOL, the same call the 07-28 pass made. The closing panorama camera grew
        with it (1600x1400 -> 1600x1560).
      - BUGS unchanged: nothing to age out (both prior FIXED entries aged out last
        pass) and all four Watches are still live.
    Zone heights checked against their contents by reading the generated file back,
    not by hand: all six fit, smallest slack 48px (zone 1), full canvas 1560x1510. Reconcile found nothing stale elsewhere: the three sessions since the
    last pass had each written their own SHIPPED entry as they landed.

**Every session that ships anything (feature, pack, or fix set) increments the counter by 1
as part of its delivery, in the same commit as its other changes.** Doc-only sessions do not
increment it and must not reset it.

**When the counter reaches 3:** that session's FIRST task is to reconcile this file and
redraw the map (see MAP PROTOCOL below), then set "Last map redraw" to today's date and the
counter back to 0. Do that before starting the feature work, so a long session can't end with
the map skipped. If the person asks to skip it, note the skip here and carry the counter
forward rather than resetting.

## MAP PROTOCOL
How and when the Excalidraw project map gets redrawn. Written so any session reproduces
the same map from this file, with or without the previous drawing's checkpoint.

**When:** when the MAP STATUS counter above reaches 3. A "shipped session" is one that
delivers a feature, pack, or fix set (doc-only passes do not count). Reconcile this file
FIRST, then draw the map from the reconciled file, then reset MAP STATUS. Never draw from
memory of a previous map.

**Zone-to-heading mapping (fixed):**
| Zone | Title on map | Source heading |
|---|---|---|
| 1 | SHIPPED — FOUNDATION | ## SHIPPED — FOUNDATION |
| 2 | SHIPPED — GAME PACKS | ## SHIPPED — GAME PACKS |
| 3 | NEXT UP (queued) | ## NEXT UP (queued) |
| 4 | FEATURES TO ADD | ## FEATURES TO ADD |
| 5 | BUG FIXES | ## BUGS |
| 6 | IDEAS — NOT SOLIDIFIED | ## IDEAS (not solidified) |

**Layout (fixed, so redraws are stable):** 3 columns x 2 rows. Columns at x=40, 560, 1080,
each 480 wide. Row 1 at y=95 (height 860), row 2 at y=980 (height 530). Zone header text
15px below zone top, fontSize 22. Items 440 wide, 40 tall, 46px step, first item 50px below
zone top; give a taller box (52-70) to any item whose label wraps past one line. Row 1 grew
from 540 to 730 on 2026-07-27 (SHIPPED FOUNDATION reached 14 items and overflowed its
zone), from 730 to 800 on 2026-07-28, when the completed pre-pack cleanup item grew
to a three-line box and left only 10px of slack, and from 800 to 860 on the third 07-28
pass, when the two newly rendered shipped sessions needed exactly the 92px of slack zone 1
had left. Each time it was raised pre-emptively rather than waiting for the next entry to
overflow it, since the fix is mechanical and the trap would have landed on whoever added
the next line.
When a zone outgrows its height again, raise BOTH row-1 zones together and push row 2 down by
the same amount, so the columns stay aligned.

**Colors:** zones 1-2 green (#d3f9d8 zone / #b2f2bb items / #15803d header). Zone 3 yellow
(#fff3bf / #ffd8a8 / #b45309); the first three items get strokeWidth 2 and a leading number,
they are the committed next sessions. Zone 4 blue (#dbe4ff / #a5d8ff / #2563eb). Zone 5 red
(#ffc9c9 zone / #b91c1c header): OPEN bugs #ffc9c9, the chosen FIX #ffd8a8, "Watch" traps
#fff3bf, FIXED items #b2f2bb. Zone 6 purple (#e5dbff / #d0bfff / #6d28d9). Newly shipped
items in zones 1-2 get #c3fae8 + "(NEW)" until the next redraw.

**Cameras:** open 600x450 on the title, then 800x600 per zone in reading order (1-6), close
on a panorama covering the full canvas (1600x1560 at the current zone heights). Title fontSize 30 at y=15, subtitle 18 at y=56 with the month/year.

**Reconcile step (do this before drawing):** move finished items from NEXT UP into the right
SHIPPED section with a one-line summary and date; renumber the top three of NEXT UP; move
fixed bugs to FIXED in BUGS and drop anything stale; add anything new discussed since the
last pass; then draw.

**Committed file copy (always, added 2026-07-19):** the live MCP canvas does not render in
Claude Code sessions (unlike regular Claude chats), so the map must ALSO always exist as
`project-map.excalidraw` at the repo root — open it at excalidraw.com. It is generated, not
hand-drawn: `scripts/generate-project-map.mjs` holds the zone/item data and emits the file
with this protocol's layout and colors. On every redraw: update the item data in that script
to match the reconciled headings, run `node scripts/generate-project-map.mjs`, and commit the
regenerated file in the same commit as the reconcile. If the MCP canvas is unavailable in a
session, regenerating the committed file alone still counts as the redraw.

## SHIPPED — FOUNDATION
- [x] Auth: 6-digit email login codes (primary) + magic links + password accounts, 30-day session cookies
- [x] 6-digit login codes (2026-07-20). Login now emails a 6-digit code with the magic link kept below it as a desktop fallback, because an installed iOS PWA runs in a browser context whose cookie jar is separate from Safari's: a link tapped in Mail logged the session into Safari and left the installed app logged out, while a typed code never leaves the app. Code TTL 10 minutes; 5 wrong attempts per code then it dies; 60 second resend throttle; one generic "Invalid or expired code" for every failure mode so accounts can't be probed. Schema: magic_link_tokens gained code + attempts columns and an (email, code) index; the token column stayed, so links already in flight kept working.
- [x] Crew: groups, invite links, join flow, roles, member removal, self-leave, crew deletion
- [x] Events + RSVPs with live updates, event deletion (cascades)
- [x] Bracket engine: single elim (derive-from-results), byes, undo cascade, scoring lock; double elim with losers bracket + grand-final reset (packages/shared/src/bracket.ts, tests via `pnpm test:bracket`)
- [x] Live sync: in-process WebSocket hub (apps/server/src/ws.ts), broadcast on every write, shared client hook useLiveUpdates
- [x] TV broadcast view (public UUID link, QR to score, live)
- [x] Lifetime stats ledger: matches/match_participants, fed by every pack on completion, retracted on undo
- [x] Quick play: running a mode without a crew creates a hidden personal crew (groups.is_personal) so scoring/TV/recap all work through one system
- [x] Recap card (canvas-to-JPG, share sheet + download)
- [x] Arcade theme across the generic app (CSS custom-property tokens + gn-* class layer) and PWA install (manifest, icons, iOS meta tags, safe-area insets)
- [x] Async crash-safe routes: async-safe.ts patches the Express Router so a rejected handler returns a 500 instead of taking down the process and the WebSocket hub
- [x] UI cleanup pass (2026-07-18). RSVP collapses to a one-line status + "Update RSVP" once answered; delete event moved inside the event tile; members moved directly under the schedule form with the invite link compacted to one copy row; password controls tucked under the name field; single-format games (Mario Party, a live Tournament) launch in one tap from the game picker; event RSVP names link to the same profile/rivalry pages as the crew list; rivalry colors flipped so YOU are always teal and the opponent red/coral. Also: generic brackets record into stats as "Tournament" instead of "Game Night" (existing rows renamed via one-off SQL in Neon: UPDATE games SET name = 'Tournament' WHERE name = 'Game Night').
- [x] Show-up confirmation + event date editing + UI cleanup pass 2 (2026-07-19). New event_attendance table (separate from RSVP intent, feeds flake tracking): once an event's date arrives the event page asks "Did you actually show? Yes/No" and the prompt disappears when answered; locked server-side until the date passes, so changing the date re-locks/re-opens it automatically. Event dates are now editable in place (creator or admin; "change" next to the date on the event page, clearing the input returns the event to TBD; draft/scheduled status follows). UI: answered RSVP collapses to a status pill inline with the event title so Games sit at the top; game night tiles put delete in a real chip-style button and moved "you: yes" onto the date/counts line; member rows swap the role chip + demote/make-admin text for a role dropdown styled as the same chip with a ▾, and remove is a matching chip button; tight rows wrap controls to a second line instead of truncating names.
- [x] Verified email sending domain (2026-07-20). mail.gamenightos.app verified in Resend (DKIM, SPF, MX feedback record, DMARC published at p=none), Cloudflare DNS points the app at Render. Login email FROM moved off the shared onboarding@resend.dev (owner-only delivery) to login@mail.gamenightos.app, with an optional reply-to; both are env-overridable (MAIL_FROM, MAIL_REPLY_TO) so they change on Render without a deploy. The 6-digit code and magic link still log to the server console in every environment as the delivery fallback.
- [x] Flake tracking + streaks + Friends (2026-07-19). Profiles gained a "show-up record": show rate, current/best show streak (🔥 at 3+), and flake count, computed from rsvps + event_attendance ordered by event date. Flake definition (revised same day, James): a flake is "RSVP'd yes and never confirmed showing up" — silence after a yes counts as a flake once the night is 24h past its date, because real flakes don't open the app to tap no; an honest "didn't show" answer counts immediately; a yes on a dateless event never counts. Home's Friends list is collapsed behind a single "Friends (n)" cabinet button so a long list doesn't clutter the page. Member pages are tabbed: tapping a crew member opens the Rivalry by default with a second "<name>'s stats" tab for just them (self view unchanged). Home gained a Friends section: everyone you've ever shared a real crew with (personal quick-play crews never count), no adding — crewing together is the connection. Tapping a friend opens /friend/:userId, the same rivalry/stats tabs aggregated across every crew you share. Server: /api/friends, /api/friends/:userId/stats, /api/friends/:userId/rivalry; rivalry + profile aggregation refactored to run over a set of group ids. No schema change.
- [x] Event share + night recap (2026-07-20). Event page gained a Share control: navigator.share when available, clipboard copy with a "Link copied" toast otherwise. The shared link routes through the existing crew invite/join flow with an event id query param (/join/:code?event=:id), so a logged-out tap lands on the join page and, after join or login, redirects to the event. No schema change: the invite code carries the event via query param, and the event detail response now also returns groupName + inviteCode so the page builds the link in one request. New route /e/:id/recap: the night recap card, aggregating every completed game under the event across every pack (Beerio, Smash, Mario Kart, Mario Party, brackets) from the one materialized ledger (matches/match_participants), with per-player rollups (wins, games, avg placement), a games-played list with each winner, and an MVP callout. Reuses the canvas-to-JPG recap pipeline (same Arcade palette, fonts, share/download path); a friendly "nothing played yet" empty state when no games exist. Members only, since guests are never materialized (deferred: showing guests needs per-pack jsonb session reading).
- [x] Profiles + rivalry cards (2026-07-18). Tapping a member in the crew list opens /g/:id/member/:userId. Yourself = profile (games/wins/win rate/best/avg + by-game table; podiums was removed 2026-07-27). Anyone else = rivalry: side-by-side stats, head-to-head record banner, per-game H2H, shareable canvas-to-JPG rivalry card. Home gained a "Your stats" card aggregating across every crew including quick play. Server: three read-only endpoints in stats.ts. No schema change.
- [x] Past game nights collapse on the crew page (2026-07-23). The crew "Game nights" list now splits on time: nights more than 24h past their scheduled date (the same grace window flake tracking uses) drop into a single collapsible "Past game nights (n)" cabinet tile, built exactly like the Friends cabinet on Home (gn-cab button, tap to expand/hide). It sits below the upcoming/current nights and above "Schedule a new one". Dateless (TBD) nights are never treated as past. Same event tile inside both lists (one shared renderer), so delete and RSVP info are identical; no server or schema change (partition is client-side on the existing events list).
- [x] Link preview + favicons + head meta (2026-07-23). Pasting the app link in iMessage/Slack/Discord now shows a real card instead of a blank rectangle. Added apps/web/public/og-image.png (1200x630, opaque RGB, plum background, the existing coral GN badge floated with a soft shadow beside the "GameNight OS" wordmark set in Luckiest Guy with a teal underline echoing the mark), generated from icon-512.png with Pillow (scripts note: the real Luckiest Guy TTF was pulled from Google Fonts to match the app wordmark). Favicons derived from the same mark: favicon-32.png (downscaled with LANCZOS, GN still legible), favicon-16.png (simplified to the bare coral square + teal bar since "GN" smudges at 16px), and a multi-res favicon.ico (16+32) for older browsers and bookmark bars. index.html head gained a meta description matching the manifest, the full Open Graph set (type/site_name/title/description/url/image + width/height/alt) and Twitter summary_large_image tags, all with ABSOLUTE https URLs (relative paths fail silently in link unfurlers), plus rel=icon links for the ico/32/16 with icon-192 kept as a larger fallback. The existing manifest, apple-touch-icon, theme-color and apple-mobile-web-app-* tags were left untouched; the manifest needed no change. No schema, no new runtime dependency (Pillow is a build-time tool only), no server change: Vite copies public/ into dist/ and Express already serves dist/ statically, so the files resolve at the site root. Per-route dynamic previews stayed out of scope (logged under FEATURES).
- [x] Keep-warm ping (2026-07-25). GitHub Actions scheduled workflow (.github/workflows/keep-warm.yml) GETs {WARM_URL}/api/health every ~13 min during evening Central hours (21:00-06:59 UTC = ~4pm to 1am CT year-round), keeping the Render free instance warm so game night never opens on a 30-60s cold start. Reads WARM_URL from a repo Actions variable set to the *.onrender.com ORIGIN (not the Cloudflare-fronted custom domain, which could answer /api/health from cache without waking Render); no-ops if the variable is unset. Evening-only, never blind 24/7: ~310 instance-hours/month, far under the free 750/month cap (24/7 would be ~744h with no margin for redeploys). The 13-min cadence is under Render's 15-min idle timeout so the service stays warm across the window, then sleeps; single fail-soft curl step; workflow_dispatch button for a manual pre-game warm. No app or schema change. Deploy: none on Render; James sets the WARM_URL repo variable once (Settings -> Secrets and variables -> Actions -> Variables). Follow-up idea logged: an event-aware warmer that only pings in the hour before a scheduled event start (fewer hours, needs an endpoint listing upcoming event start times). WIDENED 2026-07-28 to noon-2am Central (cleanup phase 8b, James picked the window option): cron `*/13 17-23,0-8 * * *`, one continuous 16-hour UTC window, 21:00-06:59 UTC before. Because the UTC window is fixed and the Central offset is not, both ends are chosen to cover the target in BOTH offsets rather than one, so each end overshoots by an hour for half the year: 17:00 UTC is noon CDT exactly and 11am CST, 08:xx UTC is 2am CST exactly and 3am CDT. ~487 instance-hours/month against the free 750, up from ~310, leaving ~260 hours of headroom for redeploys. Workflow-only change: no app code, no schema, and nothing for James to set up, since WARM_URL is already configured.
- [x] Double-elim grand-final "needs 2" clarity + TV up-next ordering (2026-07-25). The grand final now signals that the winners-bracket finalist needs one win while the losers-bracket finalist (always slot B) must win the set AND the reset. BracketPage labels the first game "Set 1", shows a "must win twice" badge naming the losers finalist, and renders the reset (GF2) as a clearly conditional game ("only if <lb> wins Set 1"), with a "no reset needed" note when the winners finalist takes Set 1. TvPage adds "Grand Final / Set 1" and "Grand Final / Reset" labels, a "needs 2" chip on the losers finalist, and an on-deck explanatory note. Shipped alongside the related item: the TV on-deck list is now ordered by depth then bracket side (winners R1, losers R1, winners R2, losers R2, and so on) with the grand final last, so the losers path is not an afterthought. UI only (the engine already models GF/GF2); no schema, no engine change. New CSS gn-bkt-gf2 / gn-bkt-gfnote and gn-tvm__needs2 / gn-tvm__note in the existing gn-bkt-* / gn-tvm-* layers. Deploy: none.
- [x] Link a guest to a crew member (2026-07-26). Owner/admin one-time backfill that credits a past typed guest's results to the crew member they turned out to be. Guests are never in the ledger (match_participants.userId is NOT NULL), so this RE-MATERIALIZES the guest's recoverable history from each pack's stored jsonb under the member's userId, reusing the live materializers via an added optional linkMap (guest display name -> userId) parameter; when linkMap is absent the materializers behave exactly as before. Covers generic brackets, Smash, Mario Kart, Mario Party, Ping Pong; Beerio was excluded at first and was added forward-only on 2026-07-27 (see the GAME PACKS entry). Preview-first: a dry run shows exactly what would be credited (per pack, each game with its label, event, date, placement) and writes nothing; confirm then writes. Additive and idempotent: the guest's already-materialized match is REUSED (not recreated) and the previously-skipped participant is inserted ON CONFLICT (matchId,userId) DO NOTHING, so re-running is a no-op and existing rows are never disturbed; the preview skips units the member already has (memberCreditedKeys per event). Exact name matching from an admin-picked list; the target is validated as a crew member server-side. New: apps/server/src/guest-link.ts (router: GET /groups/:id/guest-names, POST /groups/:id/guest-link/preview, POST .../confirm; confirm broadcasts leaderboard_updated per touched event) and apps/server/src/guest-link-util.ts (shared GuestCreditItem/Result type + the memberCreditedKeys idempotency helper); linkMap threaded through all five materializers (brackets/smash/mariokart/marioparty/pingpong); web apps/web/src/pages/LinkGuestPage.tsx at /g/:id/link-guest, reached from an owner/admin button in the crew page members card. No schema change (so db:push:ci applies nothing new). No unlink/retract in v1 (a clean retract needs a column marking backfilled rows, deferred). Deploy: none.
- [x] Per-character stats on profiles + podium stat removed (2026-07-27). Every character pack already stored the fighter/racer/character on each result (match_participants.character) and no stats screen read it; the personal profile views now do. The shared Agg helpers in stats.ts gained a byCharacter tally (keyed by character NAME, so one character is one line across titles, per the standing rule) and finishCharacters rolls it into the list plus the main (most played) and the best (highest win rate among characters clearing MIN_CHAR_GAMES = 3, so one lucky win cannot outrank a real main). Returned under a nested `characters` object rather than spread flat, because the character summary has its own `best` and the response already carries one (best placement). Rendered by one shared component (apps/web/src/CharacterStats.tsx) on MemberPage (crew member profile and the friend route, both of which run through the same Profile view) and MyStatsPage; it hides itself when the list is empty, so Beerio / Ping Pong / bracket-only profiles look exactly as before. The crew leaderboard (StatsPage) deliberately got no character section in that pass; that was REVERSED the same day, see the crew leaderboard entry below. Same pass removed the podiums stat end to end: out of newAgg/feedAgg/finishAgg, out of the /groups/:id/stats leaderboard aggregation and its Row type, and off MemberPage (profile grid and the rivalry compare), MyStatsPage and StatsPage. The recap card's visual 1-2-3 podium and Beerio's vendored podium modal are a different thing and were left alone. Read-layer and UI only: no schema change, no change to how games are recorded, live sync unaffected (read-only endpoints). Deploy: none.
- [x] Time-based stats + a match timestamp (2026-07-27). Added matches.playedAt (timestamp NOT NULL DEFAULT now()), the one thing missing before streaks and recent form could exist at all. Capture is automatic: all eight matches insert sites across every pack (brackets, Beerio, Smash x2, Mario Kart x2, Mario Party, Ping Pong) create the row inline with status "completed" and nothing ever updates a matches row afterward, so the column default IS play time and no pack sets it by hand; the guest-link backfill reuses the existing row, so a link never restamps history. Verified in the clone before relying on it. Profiles (MemberPage + MyStatsPage, the same two Session A picked) gained a Form block: current win streak with a fire marker at 3+, longest win streak, the last five results as win/place pips most recent first, and nights played. Server: the shared Agg helpers gained a timeline and finishForm (one ascending pass computes both streaks, since the run ending at the newest result IS the current streak) plus a distinct-eventId count; feedAgg moved from five positional args to one ResultRow object, because the query rows already have exactly those field names and five like-typed positional args were a swap waiting to happen. NO BACKFILL by decision: the app has not been in real use, so pre-existing rows taking the migration timestamp is meaningless and harmless. First-place finishes deliberately NOT added as a separate stat: placement 1 equals isWinner in every pack, so the existing wins figure already is it. Nights played counts distinct events off the ledger rather than attendance check-ins (see decision log). Schema deploy: db:push:ci applies the column; fallback ALTER TABLE matches ADD COLUMN IF NOT EXISTS played_at timestamp NOT NULL DEFAULT now().
- [x] Crew leaderboard gets the advanced stats too (2026-07-27). James asked for the per-character and time-based stats on the crew Stats page, reversing the scoping call that had held both to the personal profiles. The crew endpoint had its OWN duplicated aggregation loop, so rather than bolting the new tallies on twice it now uses the same shared Agg the profiles use: one newAgg/feedAgg/finishAgg per player, per game bucket too. That deleted the duplicate placement/win logic and means the crew page and a profile can no longer disagree about the same player. NUMBERS CHANGED IN ONE PLACE: crew avgPlacement used to divide by games played, while the profile divides by results that actually carried a placement, so the two disagreed whenever a pack recorded a result without ranking it. The crew page now uses the profile's definition, which is the more correct one, so an avg finish on the crew page may read slightly different from before and now matches the profile exactly. UI: every leaderboard row is expandable on EVERY tab now (it used to expand only on the overall tab), and the expanded area shows the last-five pips with current streak, best streak and nights played, plus main / best character and the top three characters. By-game stays on the overall tab only, since inside a game tab it would repeat that tab's own name. The pip visual is exported from FormStats so the crew page reuses it instead of redrawing it. Read layer and UI only, no schema change. Deploy: none.
- [x] Crew leaderboard display unified across packs + expand affordance (2026-07-27). The same player read differently depending on which tab you were on: the generic list (All, Mario Kart, Beerio, Tournament) showed wins with a played/win-rate/avg-finish subline and expanded to the advanced stats, while Smash, Mario Party and Ping Pong each drew their OWN player list with a different headline number (Smash and Mario Party wins/played, Ping Pong game wins in green), a different subline, their own ranking, and no expand at all. There is now ONE player row renderer (PlayerRows + ExpandedStats in StatsPage) used by every tab, fed by the shared per-game leaderboard, so rank, headline and expanded detail are identical everywhere. Each pack keeps the sections that are genuinely its own below the list (Smash fighters + head to head, Mario Party boards + bonus stars + characters). Per-player numbers that exist in only one pack were NOT dropped, they moved to a subline tail via an extras map keyed by userId: Mario Party total/avg stars, Ping Pong single-game wins, Smash fighter variety and the night-scoped hot streak (kept deliberately, since it measures a run within one night and the expanded block's best streak is lifetime). CONSEQUENCE: the Ping Pong tab's headline is now MATCH wins like every other pack, with game wins in the subline, and each pack tab's ranking is the shared wins/win-rate/played order rather than its old bespoke one. Also added the expand affordance James asked for: a tiny ▾ that flips to ▴ when open, on every leaderboard row and on the two cabinets that had none (Friends on Home, Past game nights on the crew page); the GamePicker and the install hint already had one, and the same ▾ is the app's existing token. Read layer and UI only, no schema change, no endpoint change. Deploy: none.
- [x] Deep personal stats + a richer head to head (2026-07-27). Three new groups of stats, all derived from data already stored, all added to the ONE shared Agg in stats.ts so the member profile, /me/stats, the friend profile and BOTH sides of a rivalry inherit them from a single implementation. Placement depth: distribution across 1st/2nd/3rd/4th-or-worse with shares, plus a last-place count. Field size is not stored anywhere, so last place is derived from one grouped count of participant rows per match and compared against the placement, one query for the whole history rather than one per match; a field of one is skipped, since its winner would otherwise qualify as last. Time and history: playing-since, a 12-month games-per-month series with empty months included so the shape is honest, games per night, best night (most wins, ties to the night that needed fewer games), and loss streaks mirrored off the existing win walk. Character depth: best and average placement per character plus a distinct-character count, still unified by name across titles. Rivalry gained the genuinely head-to-head items off the meeting map, which now carries playedAt: a signed current streak (positive is you, negative is them) so the client knows whose run it is, each side's longest run, last meeting with date/game/winner, last-five meeting pips, and each side's most-used character in SHARED matches only. Undated meetings are excluded from anything order-dependent rather than guessed at. Web: one collapsed "More stats" disclosure per profile (new Disclosure + DeepStats components), closed by default, so the always-visible tiles are untouched; the rivalry tab got the same disclosure holding a deeper side-by-side compare, and the character card gained the per-character placements and the distinct count. IMPORTANT SPLIT: finishAgg stayed sync and cheap because the crew leaderboard calls it once per player, and the two query-backed stats live in a separate finishAggDeep used only by the four personal endpoints, so the leaderboard never pays for them. Opponent/nemesis aggregates and meta-column pack extras stayed out by decision. Read layer and UI only, no schema change. Deploy: none.
- [x] Repo hygiene + a real test harness (2026-07-27, pre-pack cleanup phase 0). Deleted attached_assets/, 107 files and 44,555 lines of stale copies of App.tsx, BACKLOG.md, EventPage.tsx, BeerioApp.tsx and schema.ts, some a dozen versions deep and 2.2 MB, larger than the whole git history. The real cost was not disk: every grep and file search a session runs hit stale copies of the exact files it was about to edit, so a session could open a months-old EventPage and write confident nonsense against it. Added attached_assets/ to .gitignore so a future upload cannot silently re-commit it; the content stays in git history if it is ever wanted. Deleted GAMEPLAN.md, which had become a third source of truth that disagreed with this file (it still said "NOW: Smash Bros", listed eight standing rules against this file's ten, and duplicated rules this file owns); the two README pointers now point here. Everything in it was already covered except ONE rule, which was rescued into STANDING RULES rather than lost: SINGLE INSTANCE ONLY (numInstances: 1), because the WebSocket hub is in this process's memory and a second instance would split the room. It had been stated as a standing rule only in GAMEPLAN and a render.yaml comment. Phase 0b added a `pnpm test` script that globs every test file (packages/shared/tests/*.test.ts plus apps/server/tests/*.test.ts) instead of naming one, keeping test:bracket working, and created apps/server/tests/ with its first real test: six assertions covering async-safe.ts, chosen because its failure mode is the worst in the server (a rejected promise takes the process and the WebSocket hub down) and because it was verified by hand once on 2026-07-16 and never captured. Now 18 tests run, 12 bracket plus 6 async-safe. Still node:test via the server package's tsx, no new dependency, so the lockfile stays frozen for Render. Deploy: none.
- [x] What every request costs (2026-07-27, pre-pack cleanup phase 1). Four latency fixes, no payload change anywhere. (1) attachUser was mounted globally, so its sessions x users join ran on EVERY request a logged-in browser makes, including index.html, the JS bundle, the CSS, five favicons, the manifest, the apple-touch-icon and the OG image; a cold page load fired roughly ten needless Neon round trips before a pixel rendered. Now mounted on /api only, which is where every reader of req.user lives. (2) eventDetail ran seven queries in series when six of them only need the already-loaded event row; they now go out in one Promise.all, 7 serial round trips down to 2. This is on the hot path twice, the event page load and every RSVP, attendance and date mutation, which all return through it. The same shape in all four pack context endpoints went 5 down to 2. (3) Participant rows were inserted one at a time in a loop, so an eight-player Smash FFA was eight sequential inserts and a twelve-racer Beerio grand prix was twelve; all eight materialize sites (four packs plus generic brackets plus Beerio) now build the rows and write them in one insert through a shared insertParticipants helper. ON CONFLICT (matchId,userId) DO NOTHING behaves the same for a multi-row insert, so guest-link idempotency is unchanged. (4) sessionView gained an optional already-loaded argument, and the authed GET in each pack now reuses the row its role check just read instead of selecting it a second time. ONE behaviour change, deliberate: rows are deduped by userId before the insert, so if two guest slots typed with the same name both link to one member, `recorded` now reports 1 (the number of rows actually written) instead of 2. The old loop counted both even though the second insert wrote nothing. Deploy: none.
- [x] The missing index (2026-07-27, pre-pack cleanup phase 2). match_participants was indexed on (match_id, user_id) unique and on group_id, but nothing on user_id alone, and the unique index cannot serve a user_id-only filter because match_id leads it. Every personal stats read filters on exactly that: /api/me/stats, a member profile, a rivalry, and both friend endpoints. All of them were sequential-scanning the whole participants table. Added match_participants_user_idx on user_id, and matches_game_idx on game_id in the same pass since stats.ts joins matches.game_id to games.id on every leaderboard read. Both are additive index creations, no data change and no downtime. Done now precisely because the table is small: this is the cheap moment, not the painful one. Also logged countLastPlace's unbounded IN list as a BUGS watch rather than fixing it here (see the watch for the shape of the eventual fix). Schema deploy: db:push:ci applies both; confirm the drizzle-kit "Changes applied" line in the build log, because push --force can silently no-op in non-interactive CI. Fallback SQL: CREATE INDEX IF NOT EXISTS match_participants_user_idx ON match_participants (user_id); and CREATE INDEX IF NOT EXISTS matches_game_idx ON matches (game_id);
- [x] Stop shipping every pack to every screen (2026-07-28, pre-pack cleanup phase 4). App.tsx statically imported all 24 routes, so opening the login page downloaded and parsed the entire Beerio Kart app plus lz-string, all four pack pages, all five TV pages, the recap canvas with qrcode.react, and all five pack stylesheets. Route-level React.lazy + Suspense now splits everything you reach by choosing to go there, keeping only the entry path eager (Home, GroupPage, EventPage, JoinPage; there is no separate Login route, Home covers logged out). MEASURED: entry JS 490.67 kB down to 210.83 kB (gzip 136.22 down to 66.07), entry CSS 71.06 down to 49.62 kB, so first load roughly halved. Beerio is its own 99 kB chunk and carries lz-string with it; qrcode moved into a lazy shared chunk; each pack's CSS split into its own file automatically because the import lives inside the lazily loaded page. Two supporting pieces: beerio/BeerioRoute.tsx exists so the route component can be lazily loaded as one chunk (importing the named setBeerioNavigator from App would have pulled the vendored app back into the entry chunk and undone the split; BeerioApp itself is untouched per the 1:1 rule), and RouteBoundary.tsx catches a chunk that fails to arrive, which is a new failure mode that did not exist when everything shipped in one bundle. Its recovery is a reload rather than a retry, because a stale index.html keeps asking for a hashed chunk that a deploy removed and only a full load picks up the new one. VERIFIED BY ACTUALLY LOADING ALL 23 ROUTES headless: no test dependency was added, the pre-installed Chromium was driven over CDP from a throwaway script against vite preview, polling each route until React rendered. Every route mounted, none hit the boundary, no unexpected console errors. Pack selectors were confirmed present in their own CSS chunk (the *-root duplicates in the entry CSS are the shared safe-area inset rules index.css defines for every pack root, not a leak). Deploy: none.
- [x] Cache and compress the static build (2026-07-28, pre-pack cleanup phase 5). express.static ran with its defaults, so every asset revalidated on every load even though Vite content-hashes the filenames, which makes that revalidation buy nothing; and there was no compression anywhere in the server. Now /assets is served immutable with a one year max-age (the filename IS the version, so a change to contents changes the URL), the rest of the build gets one hour with index: false, and the SPA fallback sets Cache-Control: no-cache on index.html explicitly. That last one is the whole sharp edge: a cached index.html pins users to an old bundle because it never asks for the new hashed chunks, and index: false is what stops the second static mount serving it with the one hour max-age. Added `compression` as a dependency rather than relying on the edge, and the lockfile was regenerated in the same commit because Render's install is frozen. Justification for the dependency: it gzips API and static responses at the ORIGIN so the bundle is never served uncompressed whatever the CDN in front is doing, which matters because Cloudflare's compression is a dashboard toggle outside this repo, it does nothing if the DNS record is grey-clouded, and anything hitting the *.onrender.com origin directly bypasses the edge entirely (the keep-warm ping does exactly that, on purpose). VERIFIED against a running server rather than by reading the code: hashed asset returns public, max-age=31536000, immutable; / and a deep SPA route both return no-cache; manifest returns max-age=3600; a real GET of the bundle with gzip accepted transfers 66,065 bytes instead of 210,833, the same request without gzip accepted has no Content-Encoding, and a PNG is correctly left alone. Note that curl -I says nothing useful here, since HEAD has no body for compression to act on. Deploy: none.
- [x] Your own stats page stops disagreeing with your crew profile (2026-07-28, pre-pack cleanup phase 7). /api/me/stats returned no attendance while /groups/:id/members/:userId/stats did, so the crew profile showed your show-up record, streak and flake count and your own cross-crew page showed nothing: same person, two pages, different answer. It now returns attendance too. MyStatsPage also never rendered `best` (best placement) or `avgPlacement` even though the endpoint had been returning both since the deep-stats pass, so those are now two tiles matching the crew profile's. DECISION, as flagged: the cross-crew show-up record counts REAL crews only and excludes the hidden personal quick-play crews, because a personal crew is just you, there is nobody to have flaked on and no RSVP to break, and counting them would dilute the rate with nights that cannot be missed. Lifetime totals still include quick play, unchanged. NOT done, deliberately: the phase asked for /me/stats to be pointed at aggFor(). It already shares the aggregation (newAgg/feedAgg/finishAggDeep, since the time-stats pass), so the numbers already agree, and switching would have made things worse twice over: aggFor runs its own query, so byFormat and byCrew would need a second scan of the same rows, and aggFor filters by current memberships while the existing query filters by userId, so it would silently DROP the stats of any crew you have left (leaving deletes the membership row but keeps the participant rows). ShowUpRecord and its Stat tile moved out of MemberPage into apps/web/src/ShowUpRecord.tsx rather than being imported across pages: after the route splitting, importing it from MemberPage would have pulled that whole route's chunk into MyStatsPage's. One implementation, and MemberPage got smaller. Deploy: none.
- [x] One pack runtime on the server (2026-07-28, pre-pack cleanup phase 3). The four session packs were each adapted from the one before, so they carried the same plumbing four times: roleOf, isHostRole, loadState, saveState, ensureGame, ledgerKey, deleteMaterialized, the launch-context endpoint, the guest-name scan, the find-or-insert half of every materializer, and the sessionView/respondState pair. roleOf alone existed SIX times, counting guest-link.ts and brackets.ts. Adding a pack meant editing fourteen files and hand-copying roughly five hundred lines, with four more packs queued. New apps/server/src/pack-runtime.ts holds one parameterised set built from a per-pack config (pack, gameName, wsType, keyPrefix, table, extras); each pack keeps its own routes, request validation, state shape, materialize* and the fields its session payload adds, because the LEDGER UNIT genuinely differs (game-as-unit for Smash FFA and MK races, match-as-unit for a best-of series or a ping pong match, board-as-unit for Mario Party). MEASURED: smash 1091 -> 844, mariokart 908 -> 666, marioparty 779 -> 580, pingpong 731 -> 529, plus brackets and guest-link losing their roleOf copies; 1281 lines deleted against 371 added outside the new 552-line runtime. TESTS WERE WRITTEN FIRST and confirmed green against the ORIGINAL code before anything moved, by temporarily exporting the four private ledgerKey functions and asserting the exact strings they produced; those same strings are what the committed test now asserts against the runtime, so the test cannot have canonised the refactor. 18 tests became 55. Covered: every pack's ledgerKey (modern, legacy, idx 0 in both shapes, empty sessionKey, and two sessions on one event not colliding), all four configs field by field, the Smash/shared table split as its own assertion, distinctness of keyPrefix / wsType / gameName across packs, and the pure participant-row decision logic exhaustively (all members, all guests, a mix, a linked guest, a link map that does not name the guest, a link map that must not override a real member's userId, two slots resolving to one userId, a line naming a slot not in the roster, and which of character/score/meta ride through). No Drizzle stub anywhere: stubbing it would test the stub. THE DEFERRED HALF OF PHASE 1 CAME WITH IT, as planned: saveState now returns the view-ready payload, so all ~17 mutation handlers per pack answer from the state they already hold instead of re-SELECTing the row they just wrote, and startSession does the same for the four start routes. That was 68 careful hand-edits before this phase and was nearly free once the four disagreeing saveState signatures became one (Smash's took groupId, the others did not). wsType is now a literal union type rather than a string, so a typo is a compile error instead of silent live-sync death. VERIFIED: pnpm -r typecheck, pnpm build and pnpm test all clean, and the route table (52 routes across ten routers, method, path and handler count) was enumerated from the actual Express router stacks on both this tree and HEAD and diffed byte for byte: identical. The four sessionView payloads were compared field by field against their originals, same fields in the same order. Deploy: none, no schema change.

- [x] Make it feel fast (2026-07-28, pre-pack cleanup phase 8a). Opening the app as a returning user ran three sequential dead stages; this kills stages 2 and 3. (1) App.tsx BLOCKED THE ENTIRE APP on GET /api/auth/me behind `if (loading) return <RouteFallback />`, so every launch showed a full-screen "Loading..." before any route could mount. The Me object is now cached and read synchronously on the first render, the real UI paints immediately, and the request confirms in the background. THREE STATES, not two: `undefined` (nobody has told us yet) is distinct from `null` (known signed out), because collapsing them would flash the LOGIN SCREEN at every signed-in user on any cold cache, which after every deploy is everyone. So a warm launch paints instantly and a genuinely unknown answer waits exactly as it used to; nothing got slower. (2) New apps/web/src/cache.ts: useCachedApi<T>(key, path) -> { data, stale, loading, error, refetch, set }, memory then localStorage, returning the cached value SYNCHRONOUSLY during render rather than in an effect. A loading state now only appears when there is genuinely nothing to show. `set` is the write-through path, and EventPage is where it pays: /api/events/:id/rsvp, PATCH /api/events/:id and .../attendance all return the full updated event and were already fed to setEvent, so routing setEvent through the cache makes a return visit instant AND correct rather than instant and pre-RSVP. Applied to Home (crews, friends, your-stats tile), GroupPage (crew + nights), EventPage, StatsPage (leaderboard plus all three pack panels), MyStatsPage and MemberPage. NOT applied to the TV routes or anything under a pack's live session, deliberately. Storage is namespaced with a __BUILD_ID__ define, and foreign namespaces are swept on boot. ONE BUG CLASS FIXED WHILE WIRING IT UP: several pages checked `if (error)` BEFORE `if (!data)`, which with a cache would throw away perfectly good cached content the moment a revalidation failed; cached content now wins and the error screen is only for having nothing at all. (3) New prefetch.ts: pointerdown on a game tile, format row, crew stats button, recap link or TV link starts that route's chunk, turning the 80-150ms between finger-down and click into a head start. Same import() specifier as the route, so it is memoized, not duplicated. (4) New Skeleton.tsx plus a gn-skel CSS layer: layout-shaped placeholders on the crew list, night list, event page and stats screens, with a reduced-motion opt-out and an aria-live label since the shapes are aria-hidden. Only ever seen on a first visit. (5) The three fonts are self-hosted: six woff2 files (latin + latin-ext; Fredoka and Nunito are variable, so one file covers each whole weight range) in src/fonts, referenced from index.css so Vite hashes them into /assets and they inherit the immutable one-year caching phase 5 set up, which the Google URLs never could. That removes a render-blocking third-party stylesheet, a DNS lookup and a TLS handshake from the critical path, and makes the recap canvas font wait deterministic. COST: entry JS 210.84 -> 214.73 kB (gzip 66.07 -> 67.51). VERIFIED IN A REAL BROWSER, not by reading: Chromium driven over CDP against the real built bundle plus a stub API with a deliberate 250ms delay. Warm launch paints real data with no loading state at 120ms, well inside that delay; Home -> crew -> event -> back shows no loading state and still revalidates; a foreign build namespace is swept; every font request is same-origin; a pack chunk is absent until pointerdown and present after; a 401 lands on the login screen and clears the cache; and all 23 routes still mount without hitting the error boundary. Deploy: none, no schema change.

- [x] One pack registry, before more packs (2026-07-28). Every pack had TWO spellings and nothing reconciled them: the server's `games.pack` / `game_sessions.pack` key (smash / mario_kart / mario_party / pingpong) and the client's route segment and picker key (smash / mariokart / marioparty / pingpong). They agree for two packs and disagree for two, which is the worst possible arrangement, because every consumer only ever touched one side and so nothing ever failed loudly enough to reveal that two lists existed. The next pack was a coin flip: name it air_hockey on the server and airhockey on the client, following Mario Kart's own example, and everything compiles, the picker works, the session runs, and the recap card silently draws a blank emoji while the stats tab shows nothing. NOW: `packages/shared/src/packs.ts` holds SESSION_PACKS, one entry per pack carrying the ledger key, gameName, keyPrefix, route segment, wsType, table, display name, emoji and quick-play title; SessionPackKey, PackWsType, PACK_WS_TYPES and PACK_BY_LEDGER are all DERIVED from it, so a pack without an entry is a compile error rather than a blank space on a screen. Both sides import it. Now data-driven instead of hand-maintained: the WsEvent pack members, the four pack runtime configs (each pack file supplies only its `extras` now, via a new packConfig() helper), quickplay's four near-identical routes (one loop), the guest-link adapter table, the event TV resolver's ledger map, the event detail payload's, the recap card's emoji table, the picker catalogue's names and emoji, the event page's pack-name table, Home's hand-typed union, the eight wsType literals on the pack pages and their TV views, and the event TV's live-subscription list. TWO REAL BUGS FELL OUT, both of the exact class the registry prevents, both silent. (1) The event TV resolver and the event detail payload each hand-wrote a ledger->client table keyed "ping_pong" — a spelling that exists NOWHERE in the app, since Ping Pong's ledger key is "pingpong" — and both lookups gate on a hit, so a live Ping Pong session was invisible to the event TV (the screen sat on the lobby while a game was being played) and never showed "live now" on its tile. Shipped two commits earlier, in the TV session, which is the point: that session hit this problem on its first day and papered over it with two more one-off maps. (2) The recap card's emoji table was keyed "beerio" while Beerio writes `games.pack = "beerio_kart"`, so every Beerio night on every recap card has been drawing the fallback trophy instead of its beer since the pack shipped. Nobody noticed, because a wrong emoji does not throw. TWO THINGS DELIBERATELY LEFT MANUAL, and named in the pack checklist below rather than forced: the lazy route imports in App.tsx, because Vite needs static literals inside import() to split chunks, and the router mounts in server/index.ts, because mount ORDER is load-bearing (the public TV routers must come before the authed ones). prefetch.ts's chunk table keeps its hand-written importers for the same Vite reason, but is now typed `Record<PackKey | "beerio", Importer>`, so forgetting to register a new pack's chunk IS a compile error. TESTS: 11 new (75 -> 86). The registry's ledger keys, gameNames and keyPrefixes are pinned to the exact strings transcribed out of the pack files BEFORE the refactor — the same discipline the pack-runtime pass used, because all three fail silently when changed (a changed ledger key orphans every row already written; a changed gameName splits a pack's history across two leaderboard tabs) — plus uniqueness across every key-ish field, PACK_BY_LEDGER round-tripping, PACK_WS_TYPES covering every pack in order, the emoji lookup for all six `games.pack` values, and a named regression test for each of the two bugs above. Adding a pack goes from about fifteen files to about six, and the six are the ones that genuinely differ. No schema change, no endpoint change, no payload change. Deploy: none.
- [x] One TV button per night (2026-07-28). There was no TV entry point on the event page at all: every TV view was reached from inside the thing it shows, on an address naming a PACK rather than the night (/tv/:bracketId, /smash/tv/:eventId, /mariokart/tv/:eventId, /marioparty/tv/:eventId, /pingpong/tv/:eventId, /beerio/tv/:code). Putting a game on the big screen meant knowing which pack was being played, opening that pack on a phone, finding its TV button and getting that url onto the TV — again, by hand, every time the crew switched games. NOW: one public route `/e/:eventId/tv` (same shape as /e/:id/recap), stable for the whole night, and one `📺 TV` button on the event page, labelled with what is on when exactly one thing is on (`📺 TV · Smash Bros`; "live now" when two are running, because then the answer depends on which was touched last and this page cannot know that without asking the resolver). Never disabled: the lobby is a legitimate thing to put on the screen before the night starts, and that is when the TV actually goes on. AUTO-FOLLOW, no host picker and no stored "now showing" pointer: `GET /api/tv/event/:eventId` (new apps/server/src/tv.ts, on the public tv mount) reads the four session packs, the bracket and the Beerio room in one Promise.all, drops anything completed, and returns the most recently TOUCHED as `now`, which the client renders as that pack's OWN TV view inside itself (standing rule 3 untouched: this is a router, not a generic TV mode). RENDERS THE CHILD, NEVER NAVIGATES TO IT — a TV must not accumulate history entries, and swapping in place is what makes the Mario Kart -> Smash switch happen with nobody touching the screen. Three supporting pieces, each a real gap: (1) `brackets.updatedAt`, touched on score and undo, so a bracket being scored all night beats a Ping Pong session started later and abandoned; (2) Beerio gets the completion marker the other four packs already had (`events.beerioCompletedAt`, set at /beerio-complete, cleared when a new room opens), so a finished room ages out instead of staying "open" forever, and a crew running a SECOND tournament on the same code comes back on its own once beerio_sessions.updatedAt passes the stamp — no time window, no guessing at the vendored engine's opaque blob; (3) `event_session_changed` was broadcast from exactly ONE place in the whole server (beerio-gn's room-open route), which is why the event page never noticed a host starting Smash — it now fires from every pack's start and complete (one change in pack-runtime, so all four inherit it) and from bracket create, score and undo. `EventDetail` also gained the four session packs, so every pack tile shows "live now" like Beerio and the Tournament always could. The five TV components each gained an optional id prop falling back to the route param, so they work embedded and standalone; the six original routes stay forever for deep links, bookmarks and any QR already in the wild, and only the in-app buttons were repointed. TESTS: 13 new (55 -> 68) on the pure resolution rule, which is the half that breaks silently — nothing live, one live pack, two live (newest wins, array order irrelevant), a completed session never winning even when it is the most recent thing, Beerio stamped and not newer (including exactly-equal), Beerio newer than its stamp, a code with no room row, and identical timestamps resolving to the DECLARED tiebreak order rather than to row order, because an unstable answer makes the TV flicker between two packs and read as broken. No Drizzle stub. VERIFIED IN A REAL BROWSER against the built bundle plus a stub API, because the acceptance criteria are things nobody can check by reading: the lobby renders with names and a scannable QR when nothing is live; the Beerio board and the bracket board each render on the event TV route; flipping what is live swaps the child with the URL unchanged and NO new history entry; and the Beerio poll timer is genuinely gone after the swap (its request count frozen across 7s with a 3s poll, asserted only after first proving the poll was running, so the check means something). Schema deploy: db:push:ci applies both columns; fallback SQL in the decision log. Deploy: nothing else. FOLLOW-UP THE SAME DAY (James, after testing it on a real TV): the waiting screen now shows the NIGHT RECAP between games. It turned out to have two jobs, not one — it is on screen for the evening's first twenty minutes AND again between every game after that, and an empty "waiting for the host" is a wasted TV at exactly the moment a room looks up to see where they stand. So it splits on whether anything has been played: nothing yet keeps who's-in plus the join QR, and once there is a result it shows standings (leader crowned, wins, games, average finish) beside the latest results. It reads the SAME rollup the recap card uses — the recap route's inline aggregation was extracted to a pure rollupRecap(rows) plus an eventRecap(event, groupName) that both callers share — so the big screen and the card can never quote different numbers for the same night in the same room. The extra reads only run when nothing is live, which is exactly when the server is idle. 7 more tests (68 -> 75) on the rollup, since the ORDER of its players array is now load-bearing beyond the card: the TV reads index 0 as who is leading and puts a crown on it. Both lobby states were re-checked in the browser. No schema change, no new endpoint.
- [x] One implementation per idea on the client (2026-07-28, pre-pack cleanup phase 6, the LAST phase of the run). THREE live-sync implementations became one. BracketPage and TvPage each hand-rolled a connect/retry/visibility WebSocket instead of using the shared hook, and had already drifted: BracketPage skipped its own echo, TvPage did not. The same origin-skip filter was also written EIGHT times across the four pack pages and their four TV views, identical apart from one message-type string. useLiveUpdates.ts gained useLiveRefetch(types, field, id, refetch) plus two thin wrappers, usePackLive and useBracketLive, and eleven screens now share one implementation. The own-echo skip is now universal, which is a no-op on the read-only TV views rather than a behaviour change, because a screen that never writes never sees its own client id come back. useLiveUpdates itself stays for GroupPage and EventPage, which listen for several unrelated types and act differently on each (one navigates away when the event is deleted); Beerio's polling is correct for a vendored pack and was left alone. THE PACK-PAGE SHELL became one hook: new usePackSession<S> holds the ctx/session/loading/err/busy/reqSeq state, the refetch that Promise.alls context and session, call() with its optimistic update, snapshot rollback and newest-request-wins guard, and startSession() with the 409 confirm-and-replace dance. The four pages keep all of their own rendering, which is where packs must stay different. ONE MAP AND ONE CATALOGUE: FORMAT_LABEL existed three times (StatsPage, MyStatsPage, and recap.tsx as FORMAT_NAME) with the unit noun twice, now one src/formats.ts that all three read plus the three pack Start buttons; and the pack catalogue existed twice (quickGames in Home, eventGames() in EventPage), now one src/packs.ts with the destination, Beerio's live subline and the Tournament gating supplied by the caller. MEASURED: 599 lines deleted against 218 added across 18 files, with the four pack pages down 56-81 lines each. TWO REAL BUGS THE UNIFICATION EXPOSED, both in Mario Party, both fixed. (1) Mario Party was the only pack whose session start went through call() instead of a startSession(), so it never had the confirm-and-replace dance; and the SERVER matched, missing the `!req.body?.force` check the other three have. An in-progress Mario Party session could therefore never be replaced at all: the host got a raw 409 on the setup screen with no way forward, and the only escape was to finish or abandon the night. Both halves fixed, so standing rule 8 now behaves the same in all four packs. (2) One visible copy change, called out rather than slipped in: Smash's start button said "Start FFA" where every other surface, including the picker that launches it, says "Free-for-all". It now reads from the shared map like the rest. DECISION, contrary to this entry's own earlier note: usePackSession does NOT use the 8a cache. Phase 8a's rule that a pack's live session must never be served from cache wins, because a live pack screen is not just showing state, it is offering buttons that WRITE, and a cached roster means someone can tap a player who is no longer in the session. The pack pages are now the one place that still shows a loading state on a return visit, which is the correct trade. VERIFIED IN A BROWSER: the picker catalogue was read back out of the rendered DOM on both Home and the event page and compared tile by tile and format by format (identical, with Tournament correctly still gated on the event page), all 23 routes still mount, and the phase 8a cache checks still pass. Deploy: none, no schema change.

## SHIPPED — GAME PACKS
- [x] Beerio Kart joins guest linking, FORWARD-ONLY (2026-07-27). Beerio was the one pack the backfill could not reach: every other pack persists its full roster and results server-side, but Beerio reports a finished tournament fire-and-forget and the completion route kept only the names matching a crew member, discarding the guest names outright. Fix is at the source, not a recovery: completion now stores the FULL standings list, guests included, on a new nullable matches.rawResult jsonb ({ placements: [{ name, place }] }), and Beerio gets the same two adapters every other pack has (guestNamesBeerio / creditGuestBeerio in beerio-gn.ts, registered in guest-link.ts). SCOPE: only Beerio nights completed AFTER this deploy can ever be linked. Earlier nights are genuinely unrecoverable, and beerio_sessions.state was rejected as a source (the vendored engine's opaque shape, keyed by a reusable live-session code with no link to a finished match), so nothing is reconstructed and no credit is fabricated. The member-matching loop was extracted into one shared creditBeerioPlacements helper so the live path and the backfill can never drift; the live route calls it with no link map and behaves exactly as before. The backfill reopens the match that already exists (never creates one), passes an EMPTY member map so only the linked guest is credited (confirm writes exactly what preview showed, and a member who joined after that night is never quietly credited too), skips nights the member already has via memberCreditedKeys on the match's externalKey, and inserts ON CONFLICT (matchId,userId) DO NOTHING, so re-running credits nothing new. Preview label and date come from the completion key (b| = Bracket, g| = Grand Prix, with the embedded date, falling back to the event's scheduled date). Owner/admin gate is inherited from the shared guard; confirm's existing leaderboard_updated broadcast covers Beerio with no change. The vendored 1:1 beerio.ts was not touched. Schema deploy: db:push:ci applies the column; fallback ALTER TABLE matches ADD COLUMN IF NOT EXISTS raw_result jsonb.
- [x] Beerio Kart: 1:1 replica of the standalone app, bound to lifetime stats. Double elim + Grand Prix, spectator predictions, its own TV mode, live room bound to the event.
- [x] Smash Bros, Session A: FFA Night (2-8) + King of the Hill + character system (self-select / random / host-assign, full Ultimate roster) + its own TV mode + lifetime character stats.
- [x] Smash Bros, Session B: double-elimination surfaced in the Smash section and the generic tournament tracker.
- [x] Smashdown, a fourth Smash format (2026-07-28). Ultimate's own mode: a fixed number of battles where every fighter used is struck from the roster for the rest of the series, most wins takes it. NOT A NEW PACK — no registry entry, no route segment, no server file, no ledger key: `SmashFormat` gained a fourth member and everything else rode the Smash pack that already existed. ONE CODE PATH for 1v1 and 2-8 player series, because a battle IS an FFA game the whole roster plays, which is also what makes the cap arithmetic true. THE CAP is the part that matters more than it sounds: `floor(rosterSize / playerCount)` for the SELECTED title, so four players get 21 battles in Ultimate (86 fighters) and THREE in Smash 64 (12), and the setup screen says so as a plain sentence up front rather than rejecting a number after the host has typed it. A cap of 0 (more players than a title can feed) is a real answer and blocks the start with a sentence naming the title. THE BURN BOARD is shared, server-side, and is only ever WRITTEN by `burnedFrom(games)` — one derivation, so undo cannot leave it disagreeing with the log. Undo drops the battle, re-derives the board, and hands the undone battle's fighters back TO THE PLAYERS WHO USED THEM, so a mis-tap is replayed rather than reassigned. All three assignment modes (self / random / host) exclude the burn board AND the fighters already taken in the current battle, enforced on the server as well as the client because every mode funnels through one route and a burned pick slipping through would silently un-strike it. After a battle the picks clear and a random-assignment session rolls the next set out of what is left — except on the last battle, where they stay so every screen can still show what the series finished on. MERCY RULE implemented as a host toggle, DEFAULT OFF, and settable mid-series: it ends the series once one player's total exceeds every other player's plus all remaining battles. Strictly exceeds, deliberately — when the best a chaser can do is draw level the series is still live, because a tie is a co-win here. A clinch is reported even with mercy off, so the screen can say the lead is unbeatable without ending anything. TIES are co-winners at competition ranking (two on 1, next on 3). NEW STAT, "unique fighters won with", derived from ledger data that already existed (distinct `character` on a winning line) with no new column: on the crew Stats page's Smash panel and in the night standings on the pack page. TV VIEW is its own screen rather than a panel on the FFA one, since the burn board is the format: fighters-left count at 7vmin, the full-width struck-through board, standings and the current fighters. STATS: each battle materializes exactly like an FFA game, `format = "smashdown"` on the matches row, fighter on `match_participants.character`. NO SERIES LEDGER ROW — see NEXT UP item 1 for the deferred half and why splitting it is safe. TESTS: 21 new (86 -> 107) on the pure rules in `packages/shared/tests/smashdown.test.ts`: burn accumulation and the exact undo, the cap across four title/player combinations plus the zero case, exclusion (500 random draws never returning a burned or taken fighter, and 200 four-player assignments out of a four-fighter pool), the mercy boundary in both directions, and winner resolution with a clean winner, a two-way tie and a three-way tie. VERIFIED IN A BROWSER as well, against the real built bundle plus a stub API, because "the cap reads as a sentence" and "the burn board is legible" are not things a unit test can see: 24 checks across the setup screen, a live series waiting on fighters, a series midway with a six-fighter board, a finished series with co-winners, and the TV view in both states. No schema change. Deploy: nothing beyond the push.
- [x] Smashdown SERIES rows: winning a series is now its own stat (2026-07-28, the deferred half of the entry above, shipped same day in its own cycle). A finished series writes ONE extra ledger row labeled `smashdown`, alongside the per-battle rows rather than instead of them, and it is surfaced as "series won" on the crew leaderboard's expanded row, both personal profiles, the rivalry compare and the Smash panel. THE WHOLE DANGER IS DOUBLE COUNTING: every other match-as-unit row in this app (a Best Of set, a Ping Pong match) is the only row its games produce, so counting it counts each game once; a Smashdown series row summarizes battles that are ALREADY in the ledger, so anything that counts games and forgets to skip it hands every player a phantom game per series and the winner a phantom win, silently and forever. So the exclusion lives in ONE predicate (`isSeriesSummary`, shared) and, for the aggregation, in ONE place: the top of `feedAgg`, which all five call sites already go through, so a sixth inherits it instead of forgetting it. The tallies that live OUTSIDE feedAgg were hunted down and are named in the code: the crew leaderboard's per-format buckets and its completed-match count, /me/stats' per-format and per-crew rollups, the rivalry MEETING map (a series would otherwise be an extra head-to-head encounter on top of every battle in it), the night-recap rollup (it shares its battles' sessionKey by design, so it would have landed in their unit and reported "won 4 of 6" for a five-battle series), and the Smash panel's fighter/streak/h2h loops. PLUMBING: `ResultRow` gained `label`, selected by all four queries (two through the shared `resultCols`); `Agg` gained `seriesWins`/`seriesPlayed`, returned nested as `series` so a crew with none can hide it. THE ROW: label `smashdown`, character null (a series is not played with one fighter), co-winners all on placement 1, per-player battle wins in meta, externalKey `smash:{eventId}:{sessionKey}:series` — a literal tail, so it can never collide with a battle whatever the battle count does, while still grouping with its battles on sessionKey. `materializeUnit` gained one optional `keyUnit` for that; position still comes from idx, which is NOT NULL. WRITTEN BY RECONCILING, NOT PATCHING: `syncSeriesRow` asks whether the ledger agrees with `smashdownStatus` right now, and both halves are idempotent, so it is called from every route where `over` can flip — the battle that ends the series, an undo that takes it back, the MERCY TOGGLE (which can end a series with nothing played, the case a "write it on the last battle" rule silently misses), and complete. An abandoned series writes nothing, deliberately: unlike a Best Of set, its battles are all already recorded, so there is nothing to rescue and no honest winner to name. Guest linking credits the series row too, additively. NO BACKFILL ROUTE, and that is a decision rather than an omission: see the decision log. 11 new tests (107 -> 118). Read layer plus one new row type; no schema change. Deploy: nothing beyond the push.
- [x] Smash Bros Best Of format (2026-07-21): explicit format picker (Free For All / King of the Hill / Best Of). Best Of is a 1v1 set: pick two players, tap the winner of each game, the SET materializes once when it is won (one matches row labeled bo{N}, winner placement 1 / loser 2, fighter on character, per-player game wins in meta) via the shared series primitive. FFA and KOTH unchanged (still game-as-unit).
- [x] Mario Kart general tracking pack, now four formats (2026-07-21): Free Play (single races), Grand Prix (a cup of N races on cumulative Mario Kart points, each race materialized game-as-unit with the cup id on matches.label, cups derived by chunking the games log, cup standings read-derived and never a ledger row), Best Of (1v1 race sets, match-as-unit), King of the Hill (ported rotation). Its own MkSessionState reuses the shared primitives (series engine, kothAdvance, FFA path, Smash session factory) but the two packs stay separate. Beerio Kart is untouched.
- [x] Mario Party pack: one recorded game = one board. Pick the board, enter each player's total stars, winner is the most stars (a top tie prompts the host to tap the winner, since coins aren't tracked); optional per-player bonus stars with one exclusive owner each. Own theme, own TV view, stats tab. Schema: matches.label = board, match_participants.meta = bonus stars.
- [x] Title-scoped character selection (cross-pack): a "Which game?" selector scopes both the picker and the random pool to that title's roster. Smash: 64, Melee, Brawl, Smash 4, Ultimate. Mario Kart: MK8 Deluxe, Mario Kart World, Wii, Double Dash, MK64. Stats stay unified by character name. No schema change (titleId lives in session jsonb).
- [x] Generic bracket tracker + its TV mode + recap card (the pack-agnostic path).
- [x] Ping Pong pack (2026-07-20). Two formats: King of the Hill (winner stays, challenger line rotates per match) and Singles (host picks the two players each match). A match is free play (each single game is its own recorded result) or best-of-3/5/7, chosen at start; recording a game is one tap on the winner, the loser's points are optional (typed then tapped, never a blocking field). Best-of matches end automatically at the required game wins; in Singles free play the same two players stay teed up so each game is one tap, with a "Change players" control to switch opponents. Its own green-felt/orange-ball theme and its own TV view. Night stats: matches, wins, win rate, individual game wins, current + best win streak, and KOTH longest king reign. Lifetime crew stats panel (StatsPage, Ping Pong tab): single-game total wins (counting every individual game, including the four inside a won best of seven and each free-play game) and match wins split by format (free play / best of 3 / 5 / 7). Match wins by format read from matches.label; per-player game wins/played ride match_participants.meta. No character selector (no characters in ping pong). Doubles is explicitly out (no team model). Storage: generic game_sessions keyed by (eventId, pack='pingpong'); match_participants.meta already exists (Mario Party uses it); no schema change.
- Ping Pong format picker reorg (2026-07-21): the setup screen is one Format choice (Free Play / Best Of / King of the Hill) instead of two separate mode + match-length controls. Free Play = singles single-game, Best Of = singles series (3/5/7), KOTH = single game per match, winner stays on (its old best-of length option was dropped). Same engine and ledger underneath (PpFormat expands to mode + bestOf); both entry points (Home quick-play and the crew event launcher) offer the three formats.
- Shared primitives available to any new pack: FFA/placement roster, King of the Hill rotation (kothAdvance), best-of 1v1 series engine (series.ts: newSeries/recordSeriesGame/finalizeSeries/seriesGameTally/summarizeSeriesLog), single + double elim bracket engine.

## NEXT UP (queued)
Priority order set by James. The top three are the committed next sessions.
- [ ] 1. Smash Tournament format: a fifth option in the Smash format picker that launches a bracket from the Smash session roster and materializes with fighters. Today Smash tournaments run through the generic bracket.
- [ ] 2. Tabletop theme: second theme token block + a user-facing theme switcher (Arcade default). Foundation already laid.
- [ ] 3. (open slot — not committed. The pack candidates below are in priority order, so that is what a session should pull from unless James says otherwise)
- [ ] More game packs. Candidates: board games, darts, poker night.

## FEATURES TO ADD
Wanted, not yet scheduled into a session.
- [ ] SINGLE ACTIVE PACK + bounce-everyone-to-the-hub (the half of the 2026-07-25 unified-TV request that was deliberately NOT built on 07-28; the TV half shipped, see SHIPPED — FOUNDATION). Still open if wanted: (1) starting a pack "fully" makes it THE active pack for the event, and starting another while one is live is confirm-and-replace across packs, never two live at once; (2) on start or switch, every client currently in the event, on any screen, is bounced back (replace nav) to the event page as the hub. Both were dropped from the TV session because the TV no longer needs them: auto-follow reads what is actually happening instead of a pointer someone has to maintain, and nothing has to be enforced for the screen to be right. They are now a separate question about how PHONES should behave during a pack switch, which is worth deciding on its own merits rather than as TV plumbing. The event-page half of the original ask (item 3, a live indicator on the active pack tile) DID ship: every pack tile now says "live now" and the TV button names what is on.
- [ ] Detailed personal stats block on the Home page top-right (requested 2026-07-21). Compress the name + password fields into the left two-thirds and add a richer personal-stats panel in the top-right third (beyond today's collapsible "Your stats" card): lifetime totals, per-pack and per-format highlights, streaks/mains. Responsive: stacks on narrow screens. Sweep 2026-07-25 (checked MyStatsPage + /api/me/stats): the endpoint ALREADY returns `best` (best placement) and `avgPlacement` but the page never renders them, so those are a free add. Further candidates, in payoff order, all backed by data already in the ledger: show-up record (show rate + current/best show streak + flake count, already computed by attendanceFor and aggregatable across every crew); your main (most-won-with character across the character packs, from match_participants.character); and nemesis + victim (cross-crew head-to-head extremes: who beats you most / who you beat most). The streak LIMITATION is now lifted (2026-07-27): matches.playedAt shipped, and /api/me/stats already returns `form` (current + longest win streak, last-5) and `nightsPlayed`, so the Home block can render them straight from the same payload MyStatsPage uses.
- [ ] Mario Kart CREW-WIDE racer table on the Stats page: wins by racer across everyone (not per player) plus racer head-to-head, the two pieces of Smash's bespoke SmashPanel that the shared leaderboard does not cover. The per-player half of this request shipped 2026-07-27: the crew leaderboard already carries each member's main, best racer and top three racers, on the overall tab and inside each game tab. Build the crew-wide table only if the per-player view turns out not to be enough. (Was bundled with a Smash+Mario Kart unify idea, dropped: the two packs stay separate on purpose and only share primitives, per James 2026-07-21.)
- [ ] Mario Party: minigame head-to-heads as a second format in the pack, alongside Board night (requested 2026-07-18). Track who beats who in individual minigames; the game picker already supports multi-format packs, so this slots in as a second format entry.
- [ ] Per-route dynamic link previews (deferred 2026-07-23). Today the whole app shares one static og-image (the brand card). A shared crew or event link could unfurl with its own preview (crew name, event date, "who's in"), but that needs server-rendered per-route OG tags or an image endpoint, which means touching the SPA fallback in apps/server/src/index.ts. Left out of the link-preview session on purpose; the static brand card covers the common case.
- [ ] Smack talk feed (on the TV view and/or in-app)
- [ ] Stats on the TV view (leaderboard between matches)
- [ ] Spectator predictions ticker on the generic Broadcast (port from Beerio Kart)
- [ ] Seasons: 8-12 week arcs with standings and an offseason
- [ ] Round robin format
- [ ] Availability polling and auto-pick-the-night

## BUGS
Open first, then environment traps worth remembering, then fixed (fixed items age out after a couple of map redraws).
- Watch: delivery to brand-new recipients can be cold at first while mail.gamenightos.app builds sending reputation, and early emails may land in spam. The code and link are still logged to the server console in every environment as the fallback path when delivery is slow or misrouted.
- Watch: countLastPlace in stats.ts builds an inArray of EVERY match id a player has ever played, then compares placements against a grouped count of participants per match. The IN list grows without bound, so the query gets larger every night someone plays. It is fine at current volume and the derivation is correct; the fix when it matters is a join against a grouped subquery instead of a client-built id list, so Postgres never receives the ids at all. Logged 2026-07-27 by the pre-pack cleanup audit, deliberately not fixed in it (the index added the same day is the cheaper half of the same problem).
- Watch: the WebSocket hub broadcasts every message to every connected client. There are no rooms and no topics, so every phone in the app receives every event from every crew and filters it client-side. The filtering is correct and this is fine at friend-group scale, but it is the first thing that breaks if the app ever grows. The fix when it matters is per-event or per-crew subscriptions in ws.ts. Logged 2026-07-27 by the pre-pack cleanup audit, deliberately not fixed in it.
- Watch: `drizzle-kit push --force` silently no-ops in non-interactive CI (exits 0 without applying). Confirm the drizzle-kit success line in the build log on any schema-changing deploy; otherwise run idempotent SQL in the Neon console (ALTER TABLE ... ADD COLUMN IF NOT EXISTS).
(No fixed bugs listed: both prior entries aged out on the 2026-07-28 redraw, as this section's rule says they should. Their root causes live in the decision log, which is the permanent record.)

## IDEAS (not solidified)
Not committed, no design decided.
- [ ] Draft night mode (snake drafts for characters/teams, TV draft board)
- [ ] Wager ledger (bragging-rights bets, confirmations, outstanding debts). Bragging rights only, never money.
- [ ] Achievements/badges, including group-created custom badges
- [ ] Beer pong pack. Would force the one missing primitive, a team model (2v2), which then unlocks cornhole, foosball and doubles ping pong cheaply.
- [ ] Pool pack. Rides the same KOTH/singles match engine ping pong now uses.
- [ ] Cornhole, darts, poker night. Poker needs a new session ledger engine (buy-ins, net results) but has the highest stat payoff.
- [ ] Capacitor native wrapper: packages this web app as a real iOS/Android app (same code, native shell, push notifications). Not needed while the PWA works.
- [ ] Offline score entry sync (PWA background sync)
- [ ] Event-aware warm ping: wake Render only in the hour before a scheduled event start, instead of on a fixed clock window. Updated 2026-07-28 now that phase 8b widened the fixed window to noon-2am Central (~487 instance-hours/month of the free 750): this idea no longer buys latency, it buys HOURS BACK, and it is the only option on the table that would let the window get wider still (or reach a 3am night) without approaching the cap. Needs an endpoint listing upcoming event start times for the workflow to read. Composes with the fixed window rather than replacing it: keep a modest always-on window for spontaneous use, and let the event-aware pings cover nights that fall outside it.
## DEFERRED
- Smash character portraits: fighters are text-only for now. Portraits/thumbnails come with the UI pass (asset sourcing + licensing to think about). The roster and picker are built to swap in art without a data change.
- Smash stage tracking: which stage each game was on. Cut for Session A. Too much input per game for the payoff; the fun stat is the fighter, not the stage.
- Smash stock tracking: stocks remaining / KO counts per game. Cut for Session A for the same reason (per-life data entry). Winner + optional full placement is the low-barrier target (standing rule 9).
- House rules per group per game (versioned rule sets): deferred by James.
- Handicap engine: cut by James (not necessary).
- Venue/brewery league mode: different customer, different product. Revisit only if a venue asks.
- Money wagers: regulatory mess. Bragging rights only.

## STANDING RULES
- Read MAP STATUS at the top of this file at the start of EVERY session and increment its counter as part of any session that ships. When the counter hits 3, reconcile this file and redraw the Excalidraw project map from it (MAP PROTOCOL) BEFORE starting that session's feature work, then reset the counter. This file is the source of truth; the map is a rendering. Reconcile first, draw second, never draw from memory of the last map. A lost drawing costs one redraw; a stale backlog costs the record itself.
- Owners/admins run a mode: only they start it and edit results. Members watch. A per-mode toggle may open scoring to members, but it defaults OFF.
- Members join the HOST'S live session, never a local copy. Session state lives server-side, keyed to the event.
- Every game mode/tracker ships with a TV/spectator view, styled in that mode's OWN design language, not a generic one. Generic bracket has /tv/:id; Beerio Kart has its original live spectator QR flow.
- Every game mode/tracker screen has a BACK button (history-based, falls back to home), and a way back to the event it belongs to. Never a raw <a href> for internal navigation (full page load; iOS standalone turns it into a new Safari tab). Shared component: apps/web/src/BackButton.tsx. Beerio has its own styled one in its header.
- Completed sessions materialize into matches/match_participants. Guests are skipped until linked to a member.
- Live sync on every write (WebSocket hub, or the mode's own polling). Nobody should ever need to refresh.
- Standalone playable without a crew or event where it makes sense: typed names, no stats.
- Prefill rosters from the event's yes-RSVP list, and never clobber a session already in progress.
- SINGLE INSTANCE ONLY on any deployment (numInstances: 1 in render.yaml). The WebSocket hub lives in this process's memory, so a second instance would split the room: phones on instance A would never see updates from instance B, which breaks the live-sync rule above. Keep it at 1 until the hub moves to a shared backend (Redis pub/sub or similar). Rescued here 2026-07-27 when GAMEPLAN.md was deleted; it had been stated as a standing rule only there and in a render.yaml comment.
- Any pack with character selection has a "Which game?" title selector on its front page. The chosen title scopes the character picker AND the random pool to that title's roster (never assign a character that isn't in the game being played). Titles are subsets/variants of the series; stats stay unified across titles by character name. New character packs follow this: define the series' titles as GameTitle[] in the pack's shared module, thread titleId through the session state, and scope the picker + random with rosterForTitle().

## ADDING A PACK (the short checklist)
Most of what a pack needs is now derived from ONE registry entry. This lists what is not.

1. **Add the entry** to `SESSION_PACKS` in `packages/shared/src/packs.ts` (ledger key, gameName,
   keyPrefix, route, wsType, table, name, emoji, quickTitle). Everything data-driven follows from
   it, and the compiler finds the rest. Never change these five for an EXISTING pack: ledger,
   gameName and keyPrefix are written into the database and change nothing loudly — they just
   orphan history.
2. **Server:** the pack's own router, its state shape, its `materialize*` (the ledger unit is
   genuinely per-pack), and its `createPackRuntime({ ...packConfig("<key>"), extras })`.
3. **Mount it** in `apps/server/src/index.ts`. MANUAL ON PURPOSE: mount ORDER is load-bearing, so
   the public TV router goes with the other `/api/tv` mounts, above the authed routers.
4. **Client:** the pack page, its TV view, its CSS, and the format list in `apps/web/src/packs.ts`.
5. **Lazy routes** in `App.tsx`. MANUAL ON PURPOSE: Vite needs a static literal inside `import()`
   to split the chunk, so this cannot be looped. Add the chunk to `prefetch.ts` too — that one IS
   type-checked, so forgetting it fails the build rather than silently slowing a tap.
6. **Guest linking:** export `guestNames*` / `creditGuest*` and register them in `guest-link.ts`.

## DECISION LOG
- One exclusion, at the one place every counter already goes through (2026-07-28, the series-row session): the series summary row is the first row in this ledger that describes rows already in it, and getting the exclusion wrong is invisible — nobody spots that their games-played is one too high per series. The choice was between filtering at each reader and filtering inside `feedAgg`. It went inside feedAgg, because the crew leaderboard was already unified onto that one aggregate precisely so two screens could not disagree about a player, and putting the rule at the shared chokepoint means the NEXT caller inherits it rather than being the one that forgets. What that does not cover is the tallies that never call feedAgg, so those were hunted rather than assumed, and the hunt found one that reading the plan would not have: the RIVALRY MEETING MAP, built in the same loop as feedAgg but after it, which would have counted a series as an extra head-to-head encounter on top of every battle inside it and quietly broken both players' records. The recap rollup was the other: a series row shares its battles' sessionKey ON PURPOSE (that is what makes the deferred-backfill argument work), so it would have landed in their unit and told the room "won 4 of 6 games" about a five-battle series. Both are named in the code next to their `continue`. The regression test that matters most is the negative one: a `bo3` row must still count, because a careless "skip labelled rows" fix would delete Best Of, Mario Party and Grand Prix from everyone's stats just as silently.
- The series row is written by reconciling, and an abandoned series writes nothing (2026-07-28): `over` can flip in three places, and only one of them is the obvious one. The battle that completes the count is obvious; an undo taking it back is easy to remember; the MERCY TOGGLE ending a series with nothing played at all is the one a "write the row when the last battle lands" rule misses, and it would have shipped as "mercy ends the night but nobody is recorded as winning it". So `syncSeriesRow` asks one question — does the ledger agree with `smashdownStatus` right now — and every route that can change the answer calls it. Both halves are idempotent, so it is safe to call twice or on the wrong format. The other call: a series the host ENDS EARLY writes no row. Best Of finalizes an abandoned set to the game leader, and that is right there, because the games inside a set are never materialized and abandoning would lose them entirely. Smashdown is the opposite: every battle is already a row, so an abandoned series loses nothing, and naming a "winner" of a series that did not finish would be inventing a result rather than rescuing one. NO BACKFILL ROUTE was built, which the plan called for, and the honest reason is that there is nothing to backfill: the deferral lasted one session on the same day, so the only sessions that can predate the series row are ones started in the hours between two deploys, and those write their row the moment the host taps End (complete reconciles too). Building a one-shot admin route for zero rows is machinery pretending to be caution. If real history is ever imported, the mechanism already exists: run `syncSeriesRow` over `sessionsForGroup`.
- Smashdown is a FORMAT, and the burn board is DERIVED (2026-07-28): the two calls that shaped the session. (1) It is a fourth entry in `SmashFormat`, not a fifth pack. It shares the roster, the title selector, the character system, the FFA ledger unit, the runtime and the TV route, and the only things it adds are three state fields and a set of rules. Building it as a pack would have meant a registry entry, a route segment, a server file and a ledger key for something that is one `format` string on the rows it already writes — and it would have split Smash's history across two leaderboard tabs, which is exactly what the pack registry's own warning about `gameName` says never to do. (2) The burn board is stored on the session (the TV and the pack page both render it, and it is the centrepiece) but it is only ever WRITTEN by `burnedFrom(games)`. That is what makes undo correct by construction rather than by care: the alternative, subtracting the undone battle's fighters from a maintained set, is right until two battles use the same fighter or a legacy row carries a null, and then the board silently overstates how much of the roster is gone. Recompute, never patch — the same reasoning the KOTH undo has always used for its throne. The related rule: after a battle the picks are cleared so the next one is a fresh choice, EXCEPT on the last battle, where they stay put so every screen can still show what the series finished on and an undo has something to put back.
- Mercy fires on "exceeds", never on "can no longer be caught" (2026-07-28): the natural way to write the clinch check is "no chaser can reach the leader", and in most formats that is the same sentence. It is not the same here, because a tie at the top is a CO-WIN rather than something to break, so a chaser who can still draw level can still change the result. Mercy therefore needs the leader's total to strictly EXCEED every other player's total plus every remaining battle, and the boundary case — leader on 2, chaser on 0, two battles left — must NOT end the series. That case is a named test in both directions, because the failure is invisible: the series just ends a battle early and hands one player a win that was still a shared one. Two smaller calls in the same shape. A clinch is reported even when the mercy toggle is OFF, so the screen can say the lead is unbeatable without acting on it, which is the honest split between a fact and a rule. And the toggle is settable MID-SERIES rather than only at setup: a crew that decides at battle four they have somewhere to be should not have to abandon the night to act on it, and defaulting it off means the surprising behaviour is always the one someone opted into.
- The battle cap is a title property, said out loud before the night starts (2026-07-28): `floor(fighters / players)` looks like a rounding detail and is actually the shape of the format. In Ultimate four players get 21 battles and nobody ever meets the cap; in Smash 64 the same four get THREE. So the cap is computed from the SELECTED title's roster, the number input is clamped to it, and the setup screen states it as a sentence naming the title, the fighter count and the limit, rather than letting the host type 10 and finding out at battle four. A cap of zero (more players than the title can feed, which Smash 64 reaches at nine) is treated as a real answer and blocks the start with the same sentence, not as an error to round up to one. The server clamps independently of the client, and clamps rather than rejects: a client that sends a stale number gets the series that is actually possible instead of a 400 the host cannot act on. The related roster call: Smashdown is capped at 8 players even though the pack's roster allows 16, because everyone plays every battle (that is what makes the cap arithmetic true) and a battle is an FFA game, which `validateFfa` caps at 8. Allowing more would have produced a series that could not be recorded.
- The series ledger row was split off, and the split is safe because of the sessionKey (2026-07-28; SHIPPED the same day in the next session, which is the outcome this entry predicted): James wanted a series-level row labeled `smashdown`, kept out of headline wins and shown as its own "series won" stat. It was not in the Smashdown session, and the reason was blast radius rather than difficulty: `ResultRow` in stats.ts does not carry `matches.label`, so adding it means the five queries that build it, the `Agg` shape, `feedAgg`, `finishAgg` and five client surfaces — the highest-traffic file in the repo, in the same session as a new format. What makes deferring free rather than lossy is that every battle in one series already shares a `sessionKey` and the ledger key is `{prefix}:{eventId}:{sessionKey}:{idx}`, so the follow-up session can group battles by `sessionKey` and derive every series that has already been played. Nothing in this session touches that grouping, which is the one constraint the deferral rests on and the reason it is stated here as well as in NEXT UP. Recording this because a half-built feature with no written reason is indistinguishable from an oversight three sessions later.
- One registry for pack identity, and the two bugs that proved it was overdue (2026-07-28): the argument for this was theoretical when it was raised — two spellings, nothing connecting them, and the next pack a coin flip. Building it turned up two live bugs, both already shipped, both silent, both exactly the predicted shape. The event TV resolver and the event detail payload each mapped "ping_pong", which is not a spelling that exists anywhere in this app, so Ping Pong sessions were dropped by a lookup that gates on a hit: the TV sat on the lobby while ping pong was being played, and the tile never said "live now". The recap card's emoji table was keyed "beerio" against a `games.pack` of "beerio_kart", so every Beerio night has drawn a trophy instead of a beer since the pack landed. Neither threw. Neither would ever have been found by reading the code that contained it, because each file was self-consistent; they were only findable by putting the two sides next to each other, which is the same lesson the Mario Party 409 gap taught in cleanup phase 6. Worth recording the aggravating detail honestly: the TV session that shipped the first bug ran INTO this problem on its first day (it needed to return a pack identifier the client switches on) and solved it by writing two more one-off maps instead of stopping to build the registry. The recommendation to do the map session first was right, and taking it in the other order cost a live bug. THE LINE HELD ON WHAT NOT TO DERIVE: the lazy route imports in App.tsx and the router mounts in index.ts stay hand-written, because Vite needs static literals inside import() to split a chunk at all, and because mount order is load-bearing (public TV routers before the authed ones, or requireAuth 401s the request before it can fall through). Both are named in the pack checklist above rather than pretended away. prefetch.ts is the instructive middle case: its importers must stay literal for Vite, but typing the table `Record<PackKey | "beerio", Importer>` moves the failure from "a tap feels slower and nobody knows why" to a compile error, which is the whole game here — the goal was never to delete registration, it was to make the registration that remains impossible to get silently wrong.
- The event TV auto-follows instead of being told what to show (2026-07-28): the 07-25 request specified an active-pack POINTER on the event (a nullable column set on start and cleared on end) with the TV subscribing to it, plus a single-active-pack rule to keep the pointer honest. That was dropped in favour of reading what is actually happening: every candidate already carries a status and an updatedAt, so "the most recently touched non-completed session" is derivable with no new state, nothing to set, nothing to forget to clear, and nothing that can disagree with reality. The pointer version has a specific failure this one cannot have: a host who ends a night by closing the tab leaves the pointer set, and the TV then insists on a game nobody is playing until someone clears it by hand. The cost of auto-follow is the opposite failure, following the wrong session when two are genuinely live at once, and that resolves itself the moment anyone touches the one they care about. If the surprise ever actually bites on a real night, an override is a small addition on top; building it first would have been guessing. The single-active-pack enforcement and the bounce-everyone-to-the-hub broadcast were dropped WITH the pointer and moved back to FEATURES on their own terms, because they were only ever infrastructure for the pointer, and whether phones should be yanked between screens mid-night is a separate question that deserves its own answer.
- The TV waiting screen has two jobs, and the second one is the recap (2026-07-28, James after testing on a real TV): it was built as the pre-game holding screen and it is also what is on between every game, which is the busier of the two states and the one where people actually look at the TV. It now splits on whether the night has any results yet, and the between-games face is the recap: standings with the leader crowned, and the latest results. The implementation call worth keeping is that it does NOT compute any of that: the recap route's inline aggregation was pulled out into a pure rollupRecap(rows), and both the authed recap card and the public TV read it. A second rollup would have been easy and would have drifted, and the failure mode is uniquely bad here because both surfaces are visible AT THE SAME TIME in the same room — someone holding the recap card on their phone while the TV shows the standings, disagreeing about who is winning. The order of the players array became load-bearing in the process (the TV reads index 0 as the leader), so the MVP rule is now covered by tests rather than being an implementation detail of one card. One piece of existing behaviour the tests pin rather than change: once ANY game exists the mvp is whoever tops the ranking, so a crown can sit on a 0W row on a night where nothing has been won yet.
- Render the pack's TV view, never navigate to it (2026-07-28): the original spec said the event TV route should redirect (replace nav) to the pack's own TV url. Rendering the child in place was chosen instead, and the difference matters on a device nobody is holding. A redirect means the address bar no longer says /e/:id/tv, so the TV is now pinned to ONE pack again and has quietly lost the thing it was set up to do; recovering needs a human. Even with replace-nav, the route that redirects and the route redirected to keep swapping the history entry, and the Back button on a TV remote is not something anyone should have to think about. Rendering keeps the night's address on screen for the whole night, which is the entire feature. The consequence to hold onto: the child MOUNTS AND UNMOUNTS on every switch, so a child that leaks a timer leaks it once per pack change across a whole evening. Beerio is the one that polls on its own timer (correctly, as a vendored pack that owns its loop), so its cleanup was verified in a browser rather than by reading the code: its request count is frozen for 7 seconds after the swap, with a 3-second poll, checked only after first proving the poll had been running.
- Beerio needed a completion marker before it could be ranked at all (2026-07-28): the other four packs carry an explicit status that reaches "completed" and drops them out of the running. Beerio had an open signal (the room code on the event) and a completion signal (the /beerio-complete route that writes the standings snapshot) with nothing joining them, so a room stayed "open" forever once opened and would have won the TV for the rest of the crew's history. Three options were on the table: a recency window ("a room older than N hours is dead"), reading the vendored state blob to work out whether the tournament finished, or a stamp. The window is a guess that is wrong at both ends — it kills a long night and resurrects a dead room the next morning — and reading the blob was already rejected once, on 07-27, for the guest-linking work: it is the vendored engine's opaque shape, keyed by a REUSABLE live-session code with no link to a finished match. So: events.beerioCompletedAt, set at completion, cleared when a new room opens, and the room counts as live when the code is set and (there is no stamp OR beerio_sessions.updatedAt is newer than it). The last clause is what makes a second tournament on the same code work with no extra mechanism: the engine writes state, updatedAt passes the stamp, the room is live again. One deliberate extra rule: a code with NO beerio_sessions row never wins, because there is no board to draw and the TV would show a spinner for a room that does not exist. All of this lives in beerio-gn.ts, which is ours; BeerioApp.tsx and beerio.ts stay byte-stable per the 1:1 port rule.
- The tiebreak is declared in code, not left to Postgres (2026-07-28): two candidates genuinely can carry the same millisecond, and without a fixed order the winner depends on row order, which is not stable across queries. That is not a cosmetic problem here. The TV refetches on every relevant broadcast, so an unstable comparator makes the screen flip between two packs on consecutive refetches, and a TV that changes its mind every few seconds reads as broken rather than as a tie. The order is bracket, beerio, smash, mariokart, marioparty, pingpong, asserted in tests both ways round (including with the candidate array reversed, so the test cannot pass by accident of insertion order). Same reasoning for a missing timestamp: it sorts oldest rather than being treated as "unknown, might be newest", so a row with no updatedAt can never win by accident.
- What the bracket updatedAt default does to existing rows, stated here rather than left to be rediscovered (2026-07-28): `ALTER TABLE brackets ADD COLUMN updated_at timestamp NOT NULL DEFAULT now()` stamps EVERY historical bracket with the migration time, so for one moment every bracket the crew has ever played looks freshly touched. It does not matter, and the reason is worth writing down so nobody "fixes" it with a backfill: completed brackets are filtered out of the resolver BEFORE anything is ranked, so a finished tournament's timestamp is never compared against anything, and any bracket that is still live genuinely is the current one. The only observable effect would be on an event with two live things where one is an old un-finished bracket, which is a state that also resolves itself the moment anyone scores anything. Full deploy SQL, if drizzle-kit's push no-ops in CI (see the standing BUGS watch): `ALTER TABLE brackets ADD COLUMN IF NOT EXISTS updated_at timestamp NOT NULL DEFAULT now();` and `ALTER TABLE events ADD COLUMN IF NOT EXISTS beerio_completed_at timestamp;`
- One broadcast fixed two screens that were both quietly wrong (2026-07-28): `event_session_changed` existed in the shared WsEvent union, EventPage listened for it, and exactly ONE place in the entire server ever sent it (the Beerio room-open route). So a host starting Smash from their phone updated nothing on anyone else's event page: smash_updated fired, and EventPage does not listen for that. Nobody had reported it, because the person starting the session is looking at the session, not at the event page. Building the TV resolver forced the gap into the open, since the resolver needs exactly the same signal for exactly the same reason, and emitting it from pack-runtime's startSession and from saveState's transition to "completed" covers all four packs in one place. Brackets emit it on create, score AND undo, which is broader than the plan's "create and complete": a recorded result moves the bracket to the front of "most recently touched", and bracket_updated is keyed by bracketId while both the event page and the event TV watch by eventId, so without it the new updatedAt ranking would only ever be observed by accident. The undo case is the interesting one — it can bring a COMPLETED bracket back to life, taking it from filtered-out to the freshest thing on the night.
- The live pack session is the one screen that must NOT be cached (2026-07-28, cleanup phase 6): the backlog entry for this phase said to build usePackSession on top of the 8a cache helper so the pack pages would inherit the same instant-return behaviour as everything else. That was not done, and the reason is worth keeping. Everywhere else in the app a stale render is cosmetic and corrects itself a moment later. A live pack screen is different in kind, because it is not just showing state, it is offering BUTTONS THAT WRITE: painting a cached roster means a host can tap a player who is no longer in the session, or record a result into a session that has already been replaced, and the write lands before the revalidation arrives to say otherwise. The launch context is excluded for the same reason, since it carries the member list the setup roster is built from and the `live` flag that decides whether the page offers to start at all. So the pack pages fetch fresh every time and are now the only screens that still show a loading state on a return visit. The cost is small (you reach these screens once a night and then stay on them) and the alternative is a wrong write, which is the one failure this app cannot take back cleanly.
- Unifying four copies is how the Mario Party gap was found (2026-07-28, cleanup phase 6): Mario Party was the only pack that started its session through the generic call() helper rather than a startSession() of its own, so it silently lacked the 409 confirm-and-replace dance, and the server lacked the matching `!req.body?.force` check that the other three had always had. The combination meant an in-progress Mario Party session could never be replaced: the host got the raw 409 text on the setup screen and the only way out was to finish or abandon the night. Nobody had reported it, which is the point. A fourth hand-copied implementation does not announce that it is missing a branch the other three have; it just quietly does less, and it takes putting the four side by side to see the hole. That is the argument for this whole phase, and it is worth remembering the next time four similar screens look like they are "basically the same already".
- A cached identity is a rendering hint, never a permission (2026-07-28, cleanup phase 8a): the app no longer waits for /api/auth/me before it will draw anything, which means for a fraction of a second it can be drawing a screen based on who you were last time rather than who the server says you are. That is safe, and the reason needs to survive in writing so a later session does not "harden" it back into a blocking gate: the cached Me decides what the UI DRAWS, never what the user may DO. Every endpoint authenticates the session cookie itself, server-side, on every request, so a stale cached identity can produce a briefly wrong-looking screen and nothing else. Trading a guaranteed full-screen wait on every launch for a rare half-second of a stale display name is the right way round. The nuance that took a second pass: `undefined` and `null` must stay DIFFERENT states. Collapsing them makes a cold cache render as signed-out, which would flash the login screen at every signed-in user, and since a deploy sweeps the cache by design that would be everyone after every deploy. Flashing a login screen at someone who is signed in is worse than the spinner this phase existed to remove, so an unknown answer still waits and only a KNOWN one paints early.
- Cached content beats a failed refresh (2026-07-28, cleanup phase 8a): wiring the cache in exposed the same latent bug on several pages, which had all been written as `if (error) return <Error/>` BEFORE `if (!data) return <Loading/>`. That ordering is harmless when a page has nothing to show until it fetches, and actively wrong once it does: a revalidation failing (phone lost signal, Render still waking) would throw away a crew page or an event page that was already on screen and perfectly readable, and replace it with an error. The rule now is that data wins: the error screen only appears when there is genuinely nothing to render, and a failed background refresh just means what you are looking at is a few seconds old. This is the whole reason to have a cache on a flaky phone connection, so getting the branch order wrong would have cancelled out most of the benefit.
- The browser checks were built because the acceptance criteria were browser-only (2026-07-28, cleanup phase 8a): the phase is defined in terms nobody can verify by reading code, "the warm launch should paint the real UI with no Loading anywhere", so it was checked in a real browser instead of asserted. Chromium was driven over CDP against the REAL built bundle plus a stub API with a deliberate 250ms delay, because without the delay a fetch can resolve before first paint and hide the exact behaviour under test. Worth recording that the first run produced a FALSE PASS: the logged-out check matched "Your name" on the still-logged-in home page and reported green. The actual fault was in the harness, not the app (a `pkill -f stub-server.mjs` matched the shell running the edit and killed it, so the stub never gained its logout switch and was quietly answering 200), but a check that passes for the wrong reason is worse than one that fails, and it was only caught by reading the matched text rather than the verdict. Fixed, the same check correctly finds the login screen and an empty cache. The scripts were not committed, for the same reasons phase 4 gave: they depend on a browser path that is an environment detail, and a test needing a running preview server does not belong in the tsx unit-test glob.
- Cold-start latency: widen the keep-warm window, do not build a service worker (2026-07-28, cleanup phase 8b, decided by James). Of the three options put to him, he picked the cheapest: the ping window goes from evenings only to noon-2am Central, ~310 instance-hours a month to ~487 against the free 750. What that buys is that the app is already awake for the hours anyone actually opens it, so the 30-60 second cold start stops being the normal experience and becomes a 3am edge case. What it does NOT buy is a fix: outside the window the first request for index.html still waits for Render to spin up, and no client-side work can change that, because nothing renders before the HTML arrives. THE SERVICE WORKER WAS NOT CHOSEN and must not be slipped into a later phase as an optimisation. It is the only option that genuinely fixes a cold first load, since the shell would paint from cache while the data waits for the server, but it carries the classic footgun: a service worker that caches too eagerly pins users to an old app version, and being stuck on stale code on a game night is worse than waiting thirty seconds for a fresh one. If it is ever revisited it needs its own session with an explicit update strategy, and it would be reversing the 2026-07-18 decision on NEW grounds rather than changing our minds: that call rested on the app being online-only, which is still true of the data and no longer true of the shell. Paying for a non-sleeping tier stays available and stays James's alone. The event-aware warmer under IDEAS still composes with this and would trade some of the 487 hours back.
- What the pack runtime does NOT absorb (2026-07-28, cleanup phase 3): the tempting version of this refactor puts the whole pack behind one interface and leaves each file a config block. That was rejected. Three things stay per-pack on purpose. The LEDGER UNIT, because it is a real modelling difference and not duplication: a Smash FFA game and a Mario Kart race are game-as-unit, a best-of series and a ping pong match are match-as-unit, a Mario Party board is board-as-unit, and flattening them would either lose per-unit detail or invent it. The ROUTES and their request validation, because each pack's body shapes and 400 messages are part of its behaviour and a shared validator would drift into a lowest common denominator. And the SESSION PAYLOAD EXTRAS, since the runtime supplies the envelope (status, groupId, the state spread) and the pack supplies whatever its own screen reads. What did move is only the plumbing that was character-for-character identical across four files. The one place a judgement call went the other way is materializeUnit: the find-or-insert-the-matches-row dance was repeated six times (Smash game and series, MK game and series, MP board, PP match) and is genuinely identical, so it moved, leaving each materialize* to decide the lines and the label, which is the part that differs. Two constraints were held explicitly rather than trusted: Smash stays on smash_sessions keyed by eventId alone while the other three share game_sessions keyed (eventId, pack), because collapsing them is a data migration wearing a refactor costume; and ledgerKey output is unchanged character for character, because a changed key does not error, it just orphans the existing rows and the leaderboard quietly stops matching history. Both are asserted in tests, the second against strings captured from the old code before it was touched. Separately, wsType stopped being a `string` and became a four-member literal union: a typo'd one used to be undetectable until someone noticed a TV screen had stopped updating, and it is now a compile error.
- Tests were written against the code being replaced, not the replacement (2026-07-28, cleanup phase 3): the phase's own instruction was to confirm the tests pass against the CURRENT code first, since a test written afterwards only canonises whatever the change did. Following that literally needed a mechanism, because ledgerKey was a private function in each of the four pack files and there was nothing importable to assert against. The four were temporarily exported, a throwaway test asserted the exact strings they produced, that ran green, and only then did the refactor start; the throwaway was deleted and the committed test asserts the SAME strings against the runtime. This mattered more than it sounds: it is the difference between proving the key shape is unchanged and merely proving the new code is self-consistent. The same reasoning drove the verification at the end. Rather than reading the four files and declaring the routes unchanged, the actual Express router stacks were enumerated on this tree and on HEAD (in a throwaway git worktree) and diffed: 52 routes, with method, path and handler count, identical. Reading finds what you expect; the diff finds what you did not.
- Cross-crew show-up record excludes quick-play crews (2026-07-28, cleanup phase 7): the flake stat asks whether you turned up when you said you would, which needs someone to have let down. A personal quick-play crew is a hidden crew of one, created so scoring and TV work without a real crew; there is no RSVP to break and no night that can be missed, so including them would pad the denominator with unmissable nights and quietly inflate everyone's show rate. Lifetime totals still count quick play, because those games were genuinely played. The related call: /me/stats was NOT repointed at aggFor as the cleanup plan proposed. The stated problem behind that instruction was that /me/stats ran its own aggregation and so could disagree with the profile, but that had already been fixed when the shared Agg gained the deep stats, and repointing would have introduced two new faults: a second scan of the same rows to rebuild byFormat and byCrew, which aggFor does not return, and a silent data loss, because aggFor scopes by current memberships while the endpoint scopes by userId, and leaving a crew deletes the membership while leaving the participant rows behind. Following the instruction literally would have made your lifetime stats quietly shrink the day you left a crew. The actual defect, the missing attendance, was fixed directly.
- Route splitting needed a runtime check, so one was built rather than skipped (2026-07-28, cleanup phase 4): the phase's own risk note is that a missing Suspense boundary throws at runtime and typecheck cannot see it, so "click every route" was the acceptance criterion and there was no way to click anything from a Claude Code session. Playwright is not in node_modules and adding it would have meant regenerating the lockfile that Render installs frozen, which is exactly the dependency trap the test harness already avoids. Chromium IS pre-installed and Node 22 ships a global WebSocket, so a throwaway script in the scratchpad drove it over the DevTools Protocol against vite preview: navigate, wait for the load event, poll until React puts something in #root, then read the console. Worth recording how it went wrong first, because the failures were all in the harness and none in the app: a hand-rolled static server never executed the module script, a fixed sleep reported a different random set of routes as blank on every run, and evaluating during navigation read a destroyed execution context. The tell each time was that an EAGER route like / appeared in the failures, which a lazy-loading bug cannot explain. Only once the harness waited for the load event did it settle to all 23 green. The script was not committed: it is a one-off for this phase, it depends on a browser path that is an environment detail rather than a repo fact, and a test that needs a running preview server does not belong in the tsx unit-test glob. If route smoke-testing is ever wanted permanently it deserves a real decision about a browser-test dependency, not a leftover script.
- Deferred the mutation-side state threading to the pack-runtime pass (2026-07-27, cleanup phase 1): phase 1 asked for sessionView to take an already-loaded session AND for that to be threaded through every mutation handler, saving a read per tap. The mechanism shipped and the guaranteed double-read is gone from the four authed GETs, where loadState ran once for the role check and again inside respondState. The mutation sites did NOT get threaded, on purpose: there are roughly seventeen per pack, each would need the status literal restated by hand at the call site, and the clean version is for saveState to return the view-ready object, which cannot happen yet because the four saveState signatures still disagree (Smash's takes groupId, the others do not). Phase 3 unifies exactly those signatures, so doing it now means writing sixty-eight careful edits that phase 3 immediately rewrites, with a real chance of a wrong status literal surviving typecheck. Better to take the free win now and get the rest for nothing when saveState becomes one function. On the dedupe: batching the participant inserts forced a decision about the same (matchId, userId) appearing twice in ONE statement, which is reachable when two guests share a name and both link to one member. Rather than rely on the conflict target to sort that out inside a single command, callers dedupe by userId first. That also fixed a quiet miscount, since the old loop incremented `recorded` for an insert that ON CONFLICT had discarded.
- Deleted the third source of truth (2026-07-27, cleanup phase 0): GAMEPLAN.md, README.md and BACKLOG.md had drifted into disagreeing about the same facts, and GAMEPLAN was the stalest of the three while README sent readers to it twice. Rather than reconcile three files forever, there is now one: BACKLOG.md, which MAP PROTOCOL already declared the source of truth. Before deleting, every claim in GAMEPLAN was checked against this file, README and RENDER.md; all of it was already covered except the single-instance rule, which existed only in GAMEPLAN and a render.yaml comment and is now a standing rule here. attached_assets/ went the same way for a sharper reason than clutter: it held stale duplicates of the very files sessions edit, and a search tool cannot tell a working file from a snapshot of it, so the directory was actively feeding wrong context into every session. Deleting the working copy loses nothing, since git history keeps it, and .gitignore now stops it coming back. On testing: the harness deliberately did NOT gain a framework. The bracket tests already ride the server package's tsx precisely because adding a devDependency without regenerating pnpm-lock.yaml fails Render's frozen-lockfile install, so `pnpm test` is the same runner pointed at a glob. Test files are not in any tsconfig include, matching what the bracket test already did; they are typechecked by tsx at run time, not by `pnpm -r typecheck`, which is worth knowing before trusting a green typecheck to prove a test compiles. The first server test is async-safe rather than a throwaway placeholder, because a placeholder that asserts nothing real cannot tell a working runner from a broken one.
- Depth added to the shared aggregate, not to the pages (2026-07-27): the profile, your own stats, the friend view and both sides of a rivalry are all finishAgg over the same Agg, so placement depth, time/history and character depth were added THERE and every one of those surfaces got them at once. Writing them per page would have been four implementations that drift, which is the same trap the crew leaderboard was in before it was unified. One split was necessary though: the crew leaderboard calls finishAgg once per player, so anything costing a query cannot live in it. finishAgg stayed sync and pure, and finishAggDeep (last-place count + best night, one query each) is used only by the four personal endpoints. LAST PLACE was the interesting one: field size is not stored, so "came last" is not directly knowable. It is derived from one grouped count of participant rows per match id, compared against the placement, which keeps it to a single query for a whole history instead of one per match, and a field of one is skipped because its winner would otherwise be its last place too. Placement distribution deliberately EXCLUDES results with no placement and reports that count separately, because packs that only record a winner would otherwise pile into fourth-or-worse and read as a player who never places. Empty months are kept in the games-per-month series for the same honesty reason: a gap is part of the shape and dropping it draws a lie. The h2h streak is signed rather than labeled so one number carries both whose run it is and how long, and ordering-dependent h2h items use dated meetings only. Everything new sits behind a closed-by-default "More stats" disclosure on both the profile and the rivalry tab, which is how the minimal-by-default rule survives adding this much: the visible tiles did not change at all. Opponent/nemesis aggregates and the meta-column pack extras (Mario Party bonus stars, series records) were explicitly held for a later session.
- One player row for every pack tab (2026-07-27): asked to unify how Mario Party, Mario Kart and Smash display on the crew leaderboard. The split was structural, not cosmetic: three packs had bespoke panels built before the generic per-game leaderboard carried enough to describe a player, so each drew its own list with its own headline number and ordering, and only the generic list expanded. Now that the shared aggregation returns characters, form and nights per player per game, those bespoke player lists had no reason to exist, so they were replaced by one renderer and each panel kept only its genuinely pack-specific sections. The rule applied: a PLAYER reads the same everywhere, a PACK keeps what is unique to it. Pack-only per-player numbers were preserved rather than dropped, via a subline extras map, because unifying layout is not a licence to delete stats: Mario Party stars, Ping Pong game wins, and Smash fighter variety plus its night-scoped hot streak, which is deliberately kept alongside the lifetime streak because a run inside one night and a lifetime run are different claims. Two visible consequences accepted on purpose: Ping Pong now leads with match wins (its ledger unit) instead of game wins, and each pack tab ranks by the shared wins/win-rate/played order instead of its own. The expand affordance is the app's existing ▾ flipping to ▴, added to every leaderboard row and to the Friends and Past game nights cabinets, which had only a text hint; GamePicker and the install hint already used it, so nothing new was invented.
- Crew leaderboard reuses the profile aggregation (2026-07-27): asked to put the new character and time stats on the crew Stats page, the choice was to copy the tallies into the crew endpoint's own loop or to delete that loop and share the profile's. Shared it. The crew endpoint had been maintaining a parallel copy of the same played/wins/placement logic, which is exactly how two screens start quoting different numbers for one player, and it would have needed a third and fourth copy as stats keep landing. One real consequence, worth stating rather than burying: the two aggregations had already drifted on avgPlacement (crew divided by games played, profile divided by results that carried a placement), so unifying them CHANGES the crew page's avg finish for any pack that records unranked results. The profile's definition won because averaging placements over games that had no placement is simply wrong. Rows became expandable on every tab, not just the overall one, because the per-game character breakdown is the most useful thing here and it only exists inside a game tab. Kept compact deliberately: pips plus two summary lines inside the existing expand, not a second full stats page nested in a list.
- matches.playedAt added, time-based stats on the profiles (2026-07-27): streaks and recent form were blocked on one missing fact, WHEN a result happened, logged as a limitation twice before (the rivalry decision of 2026-07-18 and the Home stats block sweep). One column fixes it: timestamp NOT NULL DEFAULT now() on matches. NO BACKFILL, deliberately: the app has not been in real use, so the rows that exist are test data and letting them take the migration timestamp costs nothing. That is the whole reason this could be one column and no migration script. Capture is automatic rather than hand-set, and that was CONFIRMED before relying on it, not assumed: every one of the eight matches insert sites writes status "completed" inline at the moment the game finishes, and nothing anywhere updates a matches row after creation, so defaultNow() is play time by construction. The guest-link backfill reuses the existing match row and only adds a participant, so linking a guest never restamps a night. If a pack ever starts creating a pending or live matches row to fill in later, that pack must set playedAt explicitly at its completion write, or its results will be stamped when the row was created instead of when the game was played. Two shape calls. (1) NIGHTS PLAYED counts distinct eventIds off the ledger, NOT the attendance rollup, which is what the session prompt suggested. Attendance measures whether someone confirmed showing up, so a crew that never taps the check-in prompt would read "0 nights" next to 40 games, which is worse than not showing it. Having a recorded result on a night is proof you played it, needs no extra query, and is what the label actually claims. (2) FIRST-PLACE FINISHES was not added: placement 1 is isWinner in every pack, FFA and head-to-head alike, so it would be the wins number under a second name. Streaks are computed in one ascending pass, since the run ending at the newest result is by definition the current streak; results with no timestamp are excluded from the ordering rather than guessed at, and the response carries `tracked` so the client can tell "no wins yet" from "nothing ordered yet". feedAgg moved to a single ResultRow object because it had grown to five like-typed positional args that the query rows already matched by name. Accurate from this deploy forward, which is all it needs to be.
- Per-character stats on profiles, podium stat removed (2026-07-27): the character data was already being written by every character pack and read by nothing, so this is a read-layer add with no schema change and no change to how games are recorded. Three calls worth keeping. (1) UNIFY BY NAME, not by title: match_participants.character already stores a name, so grouping by it directly means Fox in Melee and Fox in Ultimate are one line, which is the existing standing rule for character stats rather than a new choice. (2) A FLOOR ON "BEST": MIN_CHAR_GAMES = 3, named and commented, because win rate over one game is 100% and would beat a genuine main every time; below the floor a character still appears in the list, it just cannot be crowned. When nothing clears the floor the card says what the floor is instead of silently showing no best. (3) NESTED, NOT FLAT: the rollup returns a `characters` object holding byCharacter / mostPlayed / best, because the character summary's `best` (best character) would otherwise collide with the response's existing `best` (best placement). The rollup lives in the shared Agg helpers, so it is pack-agnostic: any future character pack that writes the character column inherits this with no stats work. Packs with no characters write null and are skipped, and the card hides itself, so Beerio / Ping Pong / bracket-only profiles are unchanged. Scope was held to the personal profile views (MemberPage, MyStatsPage) with no per-character section on the crew leaderboard and no per-title matrix; James asked for the crew leaderboard the same day, so that half is superseded (see the crew leaderboard entry). The no-per-title-matrix call stands. Podiums were removed the same pass for the reason logged when the item was raised: a podium is a top-3 finish, and in the many 1v1 formats (Best Of, KOTH, Ping Pong singles) the LOSER places 2nd, so both players "podium" every game and the number is noise. Wins, win rate and avg placement already say it honestly. Removed from the aggregator and every stat view rather than redefined with a 4+ field-size rule, which was the alternative and costs an extra pass over match field sizes for a stat nobody asked to keep. The recap card's visual 1-2-3 podium is a layout, not this stat, and stays.
- Historical-data caveats removed from the UI (2026-07-27): the app has not been put to real use yet, in guest linking or anywhere else, so there is no pre-existing history for a warning to be about. The Beerio "forward-only, older nights cannot be credited" copy on LinkGuestPage (the intro line, the empty-preview parenthetical, and the sub-line under the credit count) was written for a history that does not exist, and warning about it would only make an admin doubt data that is fine. Removed all three, plus the now-dead beerioForwardOnly flag on the preview response (nothing consumed it). This is a copy change only: the mechanism is untouched and still reads matches.rawResult, so a Beerio night without a stored snapshot contributes nothing, silently and correctly. Standing rule going forward: do not add UI copy explaining gaps in data that predates real usage. If the app is ever seeded with genuine pre-change nights, that is when a caveat earns its place.
- Beerio guest linking is FORWARD-ONLY, fixed at the source rather than recovered (2026-07-27): the honest options were (a) parse the vendored beerio_sessions.state to reconstruct who raced on past nights, or (b) stop discarding the names going forward. (a) was rejected: that blob is the vendored engine's own opaque shape, it is keyed by a reusable live-session code with no link to the finished match, and a stale or replayed code could attach one night's racers to another night's result. That is not recovery, it is fabricating credits, and the whole feature's value is that a preview can be trusted. So the names are persisted at completion instead, and history is written off explicitly: every Beerio night finished before this deploy discarded its guest names and is unrecoverable. The UI said so at first; that copy was removed same-day once it was confirmed the app has no real usage yet, so there is no such history for it to describe (see the entry below). Storage is ONE nullable jsonb column, matches.rawResult, holding the completion snapshot { placements: [{ name, place }] }. A Beerio-only sidecar table was the alternative; a single nullable column is simpler, additive, and a future fire-and-forget pack could reuse it. It is documented as Beerio-only today because every other pack already persists its full session jsonb (brackets, smash_sessions, game_sessions) and has nothing to gain from it. The member-matching loop was extracted into one shared creditBeerioPlacements helper rather than duplicated, so the live completion path and the backfill can never disagree about what makes a name a member; the live route passes no link map and is behaviorally unchanged. One deliberate asymmetry: the backfill passes an EMPTY member map, so only the linked guest is ever credited. Passing the real member map would have let a member who joined the crew AFTER that night get silently credited by a link that previewed nothing of the sort, which would break the preview-is-the-contract guarantee. Idempotency is unchanged from the rest of the feature (reopen the existing match, skip via memberCreditedKeys, insert ON CONFLICT (matchId,userId) DO NOTHING). Schema deploy: db:push:ci; the column is nullable with no default, so existing rows get NULL and are correctly invisible to guestNamesBeerio.
- Link-a-guest shipped as a one-time additive backfill (2026-07-26, supersedes the 2026-07-25 deferral below): no schema change, no persistent link table, no forward auto-credit (once the guest becomes a member they are picked as a member, so forward auto-credit buys nothing, and a permanent name binding could miscredit a different real person later typed with the same name). Owner/admin only, enforced server-side. Preview-first is the safety net because nothing here is runtime-tested in CI (Render only typechecks; no node in this environment): a dry run returns exactly what would be written and writes nothing, confirm then writes. Mechanism: an optional linkMap (guest display name -> member userId) added to each of the five recoverable materializers; when absent the live path is byte-identical to before. When a guest slot's name is in linkMap, the row materializes under that userId into the match that ALREADY exists (reused, never recreated, since materialization is per-game on record) via ON CONFLICT (matchId,userId) DO NOTHING, so it is additive, idempotent, and never disturbs existing member rows. Confirm reuses the materializers; the preview reads the stored lines (placement is read, never recomputed) and skips units the member already has (memberCreditedKeys queries the member's existing external_keys per event; brackets check the bracket's one match directly). Beerio is excluded (opaque vendored jsonb). Unlink/retract deferred to v2 (a clean retract needs a column marking backfilled rows to tell them from natural ones). Exact-name matching from an admin picker, no fuzzy matching (a name that does not exactly match a member's display name is precisely what makes something a guest).
- Link-a-guest deferred after investigation (2026-07-25, design-first task): mapped where guest names live and how each pack materializes (see the FEATURES scope note). Core finding: match_participants.userId is NOT NULL + FK, so guests have zero ledger rows; crediting a guest is re-materializing their history from each pack's stored jsonb, not re-pointing rows. Recoverable in generic brackets (Entrant guest shape, idempotent by bracketId) and in the four session packs (SmashPlayer / PpPlayer rosters in jsonb, keyed {pack}:{eventId}:{sessionKey}:{idx}); Beerio is opaque (names matched at completion) and stays out. Recommended safe mechanism is additive backfill (insert the member's participant per already-materialized match, ON CONFLICT (matchId,userId) DO NOTHING), never delete+remat. NOT built this session: it spans five heterogeneous materialize paths plus a live stats-ledger mutation and is untestable in the Claude Code environment (no node; the Render build only typechecks). A brackets-only slice would be clean but a misleading partial (an admin would expect all history credited, not just tournaments), so the item is left whole in FEATURES rather than shipped half. Next session that picks it up should build the bounded owner/admin rebind (guest name + member, show affected games, additive backfill, Beerio excluded).
- Every ledger row carries its format; recap redesigned to Style A (2026-07-21): added matches.format (additive column) stamped by every pack at materialization (free / bestof / koth / ffa / grandprix / board), because label alone couldn't tell formats apart (Ping Pong free play and KOTH both label bo1, which is why KOTH counted as Free Play). This unlocked: (1) per-format lifetime stats, a "Wins by format" breakdown on each pack's crew Stats tab, correct across every pack; (2) the night recap grouping matches into the thing actually played (one Best Of run, one KOTH run, one Grand Prix cup) via a sessions array. The recap card was rebuilt to the James-approved Style A "Podium Night": MVP hero, a 1-2-3 podium, then a line per session with its winner and how dominant (won X of Y races/sets/games). No "best of 1" bucket: free play IS best-of-1, so the buckets are the formats people pick. Schema deploy: db:push:ci applies the column; fallback ALTER TABLE matches ADD COLUMN IF NOT EXISTS format text. Pre-existing rows have null format (bucket "Other").
- Smash and Mario Kart stay TWO separate packs, sharing primitives only (2026-07-21, James): the queued "unify into one config-driven session pack" idea is dropped for good. They keep separate routers, pages, tables (Smash on smash_sessions, Mario Kart on game_sessions) and TV designs, but reuse shared building blocks: the best-of series engine (series.ts), the KOTH rotation (kothAdvance), the FFA placement path, and the Smash session factory. Mario Kart got its own MkSessionState (the Smash session shape with MK's own format union plus Grand Prix bookkeeping) so the two type systems never entangle.
- Explicit per-pack format pickers (2026-07-21): each pack now exposes its formats as one Format choice at session start, replacing ad-hoc mode flags. Ping Pong: Free Play / Best Of / KOTH. Smash: Free For All / King of the Hill / Best Of. Mario Kart: Free Play / Grand Prix / Best Of / KOTH. The picker value is stored in session state (PpFormat/SmashFormat/MkFormat) and expands to the engine's mode + bestOf; the ledger keys off mode/bestOf as before, so stats and history are unchanged. One games row per pack, never one per format (format lives in state and, where it must survive, in matches.label). Both entry points (Home quick-play and the crew event launcher) always carry the same formats via ?format=.
- Best Of is match-as-unit across every pack (2026-07-21): a best-of set is one ledger match (label bo{N}, winner placement 1 / loser 2, per-player game wins in meta), materialized once when the set is won, and finalized to the game leader if the night ends mid-set (a dead tie stays unrecorded). Smash and Mario Kart use the shared series primitive; Ping Pong keeps its own equivalent (left untouched since it works).
- Grand Prix cups are DERIVED, not stored (2026-07-21): a cup is raceCount consecutive races, computed by chunking the games log (race i belongs to cup floor(i/raceCount)+1). Each race still materializes as its own ledger match (game-as-unit) tagged with its cup id on matches.label; there is deliberately no cup-level match row, so a "win" in the lifetime ledger keeps meaning one race. Cups advance automatically every raceCount races and cup standings (cumulative MK8 points) are read-derived, which makes undo trivial (nothing to unwind but the race itself) and avoids a stored cup pointer that could drift.
- Starting a format over a live session is confirm-and-replace (2026-07-21): the start route 409s when a session is already in progress (has recorded games or series); the client shows a confirm and, on yes, resends with force to replace it. Never a silent clobber (standing rule 8). External-key namespacing per session (the sessionKey work) means the replaced and replacement sessions never collide in the ledger.
- Ping Pong ledger key is namespaced per session (2026-07-21): materializeMatch keyed each match pp:{eventId}:{idx}, and idx restarts at 0 every time a session starts (new state, matches:[]). So the SECOND session on the same event (the "That session wrapped. Set up another one below." replay a crew does on a recurring event) produced keys that collided with the first session's, and the dedup SELECT dropped every new match as a duplicate: matches silently stopped recording to the recap and leaderboard after the first night, while the first night's numbers lingered. Fix: PpSessionState now carries a sessionKey (set in newPingPongState), and the key is pp:{eventId}:{sessionKey}:{idx}; legacy sessions with no sessionKey keep the old shape and never collide with new ones. Verified three back-to-back sessions on one event all record (recap totalGames 3), and undo still deletes the right row. Smash, Mario Kart and Mario Party materialized with the same {pack}:{eventId}:{idx} shape and had the same latent bug on event replay; they were given the same sessionKey treatment on 2026-07-21 (Smash and Mario Kart share newSmashState, so one change covers both; Mario Party via newMpState). Verified with two back-to-back Smash sessions on one event both recording. Timely because best-of formats are coming to Smash and Mario Kart. This does not touch existing rows: any duplicate/stale ledger data already in Neon from earlier testing is separate and needs a manual cleanup, which I can't do without the production DATABASE_URL.
- Ping Pong finalizes an in-progress match on session end (2026-07-21): a best-of match only materializes to the ledger on a decisive finish, so a match abandoned mid-set (the host ends the night at, say, 2-1 in a bo5) was losing every game played in it, and those bo3/5/7 results never reached the night recap or the leaderboard. Free play never hit this because every single game completes immediately. Fix: the complete route now calls finalizeCurrent(), which awards the current match to whoever leads on games and materializes it; an exact game tie (2-2 in a bo5) has no fair winner so it stays unrecorded. The host controls show a hint that ending records the current match for the leader. No schema or Neon change: same materialize path, and the schema already auto-syncs on deploy via db:push:ci. Also made the recap card render the match length in words (Best of 7) instead of the raw bo7 token.
- Ping Pong match win vs game win are distinct stats (2026-07-20): because the ledger unit is the match, a best-of-seven win and a free-play single-game win both count as one match win, which is not the same achievement. So individual game wins are tracked too: a player's game wins total every game they won inside their matches (up to four in a won bo7) plus every free-play game. Match wins are additionally split by match length (free play / bo3 / bo5 / bo7), which only differs across sessions since a session is one length. Implementation reuses what the ledger already stores: the format lives in matches.label, and per-player game wins/played are written to match_participants.meta at materialization (the meta jsonb column already exists for Mario Party, so no schema change). Surfaced as a Ping Pong lifetime panel on the crew Stats page, same pattern as Smash and Mario Party. Note: ping pong matches recorded before this change carry no meta, so they contribute 0 game wins; there is no real ping pong data yet, so no backfill is needed.
- Ping Pong ledger unit is the MATCH (2026-07-20): one completed best-of-N match materializes one matches row plus two match_participants rows (winner placement 1, loser 2). The individual games and any optional points live only in the session jsonb. Rationale: in KOTH the challenger line rotates per match, not per game, so match-as-unit keeps win rate, streaks, and king reigns meaning what people say out loud. This deliberately differs from Smash/Mario Kart, which record each game. Match length rides matches.label (bo3/bo5/bo7); each player's optional points ride match_participants.score (their total points in games they lost, the only points captured; null when none entered). No schema change: additive use of game_sessions + matches/match_participants, same path Mario Kart uses. (This was the ledger-unit decision flagged for James to confirm; the interactive confirm was declined with "continue", so I proceeded with the proposed default and am recording it here.)
- Ping Pong doubles is explicitly out (2026-07-20): 2v2 needs a team model the per-player match_participants ledger does not have. Nothing in the pack builds toward it, no half a team model was added "for later". Doubles ping pong stays parked behind the Beer pong pack's team-model work.
- Ping Pong optional points are entered then tapped (2026-07-20): the loser-points input sits with the two one-tap winner buttons and is submitted together with the winning tap, rather than editing a game after it is recorded. This keeps recording to one endpoint and one tap with zero friction to skip (leave the field empty), and avoids editing a completed match's frozen games. Points are optional flavor and are not used in any stat (no point differential, no ELO, per scope). Minor deviation from a literal "tap first, then optionally enter" flow, flagged here.
- Night recap MVP rule (2026-07-20, confirmed by James): MVP of the night is most wins, tiebreak by best (lowest) average placement (the avg stat shown on the card). A player with no ranked placement sorts last on the tiebreak; a remaining exact tie falls to alphabetical name only as a stable, non-ranking fallback. Implemented in rankMvp() in apps/server/src/events.ts. (The games-played tertiary tiebreak from the original proposal is not used.)
- Night recap is ledger-based, members only (2026-07-20): the recap aggregates the materialized matches/match_participants ledger, which is the single cross-pack source every pack writes into on completion. Guests are never materialized (they have no userId to credit, existing rule), so they do not appear on the recap. Showing guests would require reading each pack's live jsonb session (smash_sessions, game_sessions, beerio_sessions) with three different shapes; deferred rather than built this session. Flagged as scope, not silently dropped.
- Event share routes through the existing invite/join flow (2026-07-20): rather than a new public event route or a schema change, the share link is the crew invite link with an event id query param (/join/:code?event=:id). JoinPage reads ?event= and, after the idempotent join, redirects (replace navigation) to /e/:id; the event param is carried through login so a logged-out user returns to the join page and then the event. The invite code already identifies the crew, so no token schema change was needed.
- Login email sends from a sending SUBDOMAIN (2026-07-20): FROM is login@mail.gamenightos.app, on mail.gamenightos.app rather than the root gamenightos.app. A login flow generates bounces and spam complaints (mistyped addresses, "not me" reports); keeping that on a dedicated sending subdomain means the bounce reputation can never drag down the deliverability or trust of the root domain the app itself is served from. FROM and the optional reply-to are both env-overridable (MAIL_FROM, MAIL_REPLY_TO) so the addresses can change on Render without a deploy, and reply-to is omitted entirely when unset rather than pointed at an invented mailbox.
- 6-digit code login (2026-07-20): added a typed 6-digit verification code as the primary passwordless path, because an installed iOS PWA runs in a standalone browser context whose cookie jar is separate from Safari's. A magic link tapped in Mail opens Safari and sets the session cookie there, leaving the installed app still logged out; a code typed into the app keeps the cookie in the app's own context. One magic_link_tokens row now carries BOTH the code and the existing link token (additive columns code + attempts, index on (email, code); the token column and the tap-through link flow are untouched, so links in flight keep working). Decisions: code TTL 10 minutes (both code and link on a row share this expiry, down from the link's old 15); the email shows the code large and monospace above the fold with the link kept below under "Or log in on this device", both working per decision 2; 5 wrong attempts per row burns the code (row marked used); 60 second resend throttle enforced server-side and mirrored by the UI countdown. verify-code returns one generic "Invalid or expired code" for every failure mode (wrong / expired / capped / no such email) with no account probing; request-link returns { ok: true } regardless of account existence and silently no-ops within the throttle window, though a malformed-format email still gets a 400 so a typo is caught (that reveals nothing about accounts). find-or-create + session start was extracted into a shared loginVerifiedEmail() used by both POST /verify and POST /verify-code. Code generation uses crypto.randomInt (not Math.random), zero-padded so 004217 is valid. This is code only; DNS and Resend domain verification are handled separately by the owner. Navigation on code success reuses the existing password path's finish() (a same-origin window.location reload, which stays inside the installed PWA scope); the "use the router" guidance was noted but reusing finish keeps the two paths identical and avoids threading login state through App/Home, flagged in the session summary.
- Mutation latency pattern (2026-07-19): interactions were paying three trips (POST -> load() refetch -> WS-echo refetch) with buttons locked throughout. New pattern, applied to EventPage RSVP/attendance/date first: (1) optimistic local update on tap with snapshot rollback + alert on failure; (2) mutations return the FULL updated detail payload (shared eventDetail() builder serves the GET and every mutation), which the client applies directly — no load() after a write; (3) every request carries a per-tab X-GN-Client id, broadcast() stamps it as msg.origin, and the acting tab skips messages bearing its own origin while all other clients reload as before. A useRef request-sequence guard drops out-of-order mutation responses under rapid taps. Measured: 0 redundant refetches after a write (was 2) and ~40ms perceived repaint. Extended app-wide the same day: Smash, Mario Kart, and Mario Party mutations return the full session view via a shared sessionView() (same shape as the GETs, so they can never disagree) with saveState() threading origin; fighter/character picks are optimistic; record/undo/start apply the response with busy kept as double-tap protection. Brackets: score/undo/settings return the re-derived view and BracketPage's raw socket skips own-origin frames. groups.ts, events create/delete, and beerio-gn stamp origin so GroupPage (which already applies its writes locally) skips its own echoes. Beerio's vendored pack is deliberately untouched — it has its own polling sync. Verified live: 0 extra GETs on start/pick/record in Smash, ~20ms optimistic fighter pick, cross-client broadcast convergence intact everywhere.
- Flake definition (2026-07-19, James): a flake is RSVP yes + never confirming arrival, not just an explicit "didn't show" answer — nobody opens the app to confess a no-show. Silence after a yes counts once the event is 24h past its date (grace so the prompt can still be answered the night of); an honest "no" answer counts immediately; a yes on a dateless (TBD) event never counts, and shows always count the moment they're confirmed. Implemented entirely in the stats read path (attendanceFor), so the definition can be tuned without touching stored data.
- Friends live behind one button on Home (2026-07-19, James): the list will grow unbounded, so the section renders as a single "Friends (n)" cabinet button that expands to the list on tap.
- UI cleanup pass details (2026-07-18): rivalry color convention is YOU = teal, opponent = red/coral, applied on the rivalry page and the share card (the card previously had it the other way around). RSVP collapses to a one-line status + "Update RSVP" once answered; show-up confirmation will collapse the same way when built. The game picker skips the format expansion for any game with exactly one format (Mario Party today; also a Tournament tile once its bracket is live), so those are one tap. Delete event moved inside the event tile (stops navigation via preventDefault/stopPropagation). On the crew page, Members sit directly under the schedule form and the invite link collapsed to a single copy row; the raw URL only appears if the clipboard is blocked, preserving the long-press fallback. Event RSVP names now link to /g/:id/member/:userId like the crew list does.
- Map cadence is counter-driven, not memory-driven (2026-07-19): "every third session" is unenforceable across chats, since a fresh session cannot know the count. The MAP STATUS block at the top of this file holds the tally and the last redraw date; every shipping session increments it, and the session that tips it to 3 redraws the map before doing its feature work. The counter lives in the repo, so the rule survives losing a chat, the drawing, and the project settings all at once.
- Backlog structure mirrors the project map (2026-07-19): headings were reorganized so each one renders as exactly one zone on the Excalidraw map (SHIPPED — FOUNDATION, SHIPPED — GAME PACKS, NEXT UP, FEATURES TO ADD, BUGS, IDEAS). MAP PROTOCOL at the top pins the layout, colors, and cameras so any session redraws the same map from this file alone, with or without the previous drawing's checkpoint. Renaming or reordering these headings means updating MAP PROTOCOL in the same commit.
- Event date editing + arrival confirmation are ONE unit of work (2026-07-19): arrival confirmation is gated by the event date, so the date has to be authoritative and mutable before the gate can be trusted. Changing a date re-evaluates the lock. Building them separately means writing the lock twice.
- Magic link failure is unconfirmed as a Render regression (2026-07-19): Resend's free tier only ever delivered to the account owner's address, which is indistinguishable from "broken" when a friend tests it. Check the Render logs for the generated link before treating this as a delivery bug. The verification-code fix is worth building regardless, because it also resolves the installed-app cookie split that was already on the backlog.
- Standalone web app pass (2026-07-18): added manifest + icons + apple meta tags so Add to Home Screen launches without browser chrome; status bar style is black-translucent with safe-area insets padded on every root shell (gn-app, gn-tv, beerio-root, mk/mp/sm roots and TVs) so content stays clear of the notch and home indicator while backgrounds bleed edge to edge. Shells that carried Tailwind padding utilities moved that padding to inline calc(base + env(safe-area-inset-*)) because un-layered CSS and utility classes fight over the same property. No service worker / offline mode: decided against for now (app is online-only by nature). Magic-link-in-standalone gap logged in NEXT.
- Mario Party titles, bonus-star accuracy, exclusivity (2026-07-18, revises the entry below): three titles added (Super Mario Party 2018, Mario Party 6, Mario Party 2), so the picker is Jamboree (default), Superstars, Super Mario Party, MP6, MP2. The original bonus-star sets were WRONG and are corrected: modern games renamed Coin Star to Rich Star/Rich Bonus and Happening Star to Eventful Star/Eventful Bonus, and Superstars/Jamboree support nine bonus stars each (the "On" setting), not three (three is the "Classic" setting). Verified per game against the Mario Party Overlay bonus list plus the Super Mario Wiki. Counts: Jamboree 22 chars/7 boards/9 bonus, Superstars 10/5/9, Super Mario Party 20/4/11, MP6 11/6/3 (MP5's ten plus Toadette; Toad is NOT playable in 6), MP2 6/6/3. Each title shows its OWN award names while recording (what's on screen), but lifetime stats aggregate by FAMILY via MP_BONUS_FAMILIES/bonusFamilyOf, so Coin Star + Rich Star + Rich Bonus all roll up to "Coins" and the crew's "who always wins the minigame star" holds across every Mario Party they own. Same one-identity-many-spellings principle as characters. Bonus stars are now EXCLUSIVE per board: the recording UI was flipped from per-player chips to one row per bonus star with a single owner (structurally impossible to double-assign, and far fewer taps: 9 rows instead of 36 chips on Jamboree), and rankMpLines rejects a duplicate award server-side too. One player can still hold several different bonus stars. Toggling a player out of a board releases any award they held. Mario Party also added to the home Quick Play picker.
- Mario Party pack (2026-07-18): a recorded "game" is one BOARD, not a minigame or a turn. Input is deliberately minimal (standing rule 9): board, each player's final star count, and optional bonus-star toggles. Coins are NOT tracked, so a tie on stars can't be auto-resolved; the host taps the winner instead (rankMpLines rejects a tapped winner who isn't tied at the top). Capped at 4 players, the game's own limit. Two new nullable columns rather than a pack-specific table: matches.label holds the board and match_participants.meta holds { bonusStars: [...] }, both generic enough for future packs (darts legs, poker tables). Stars ride on the existing match_participants.score. Title selection (Session 1's pattern) scopes three lists here, not just the roster: characters, boards, and bonus stars, since those differ per title (Jamboree awards Event Star, Superstars awards Happening Star). Jamboree is the default (newest, widest). A "Custom board" option is always available so a board we didn't pin, or a house variant, still records. Stats live on their own /groups/:id/marioparty-stats endpoint, leaving the generic aggregator untouched (same call as the Smash pack).
- game>format navigation (2026-07-16): the "start a game" surface on both the home quick-play screen and the event page is now a two-step "pick a game, then a format" chooser (apps/web/src/GamePicker.tsx, shared by both). Games: Mario Kart (formats: Beerio Kart, General tracking), Smash Bros (FFA, King of the Hill), Tournament (Single elim, Double elim — the "format is the game" case, so no distinct game name). Labels changed "Game modes"->"Games" (home) and "Tournament"->"Games" (event). The format choice carries via query (?mode= into Smash, ?format= into Quick Play); session packs (Beerio/Smash/Mario Kart) are plain links that gate hosting on their own pages, so only Tournament needs host/RSVP gating in the picker (an owner/admin with 2+ yes RSVPs sees single/double; members see "waiting", an existing bracket shows "open").
- Title-scoped character selection (2026-07-17): "From" means the specific title being played, not a character's origin franchise. Playing Brawl gives you Brawl's roster with no Ultimate newcomers; playing Mario Kart World gives that game's cast. A "Which game?" dropdown sits on each character pack's front page and scopes both the picker and random assignment (host asked for random to stay within the game being played). Implemented in packages/shared as GameTitle[] (SMASH_TITLES, MARIO_KART_TITLES) + rosterForTitle(); titleId lives in the session state, so no schema change. Smash earlier titles are subsets of the Ultimate roster so spellings and stats stay identical (Everyone-is-Here makes that exact); counts checksum to canonical totals (64=12, Melee=26, Smash 4=58). Mario Kart World = the 24 main characters only, excluding the 26 enemy/NPC transform racers as low-value noise (same reasoning as Smash's per-Pokemon cut, standing rule 9). Stats are NOT split by title (James chose the simple leaderboard); character is character across titles. Default title is newest/widest (Smash Ultimate, Mario Kart 8 Deluxe). The random helpers already took a pool arg, so scoping was just passing rosterForTitle(...). Per-title rosters are the most likely spot to want a small correction later; each is a one-line data edit.
- Mario Kart general tracking pack (2026-07-16): Beerio Kart now lives under a "Mario Kart" game heading alongside a new "General tracking" format. General tracking is FFA races built on the SAME pure session engine as Smash FFA (packages/shared session logic: roster, per-game placements/winner-only, night summary) pointed at a Mario Kart 8 Deluxe roster (packages/shared/src/mariokart.ts), race wording, no KOTH. Storage is a new generic game_sessions table keyed by (eventId, pack) — additive, so smash_sessions is untouched and Smash keeps working; new packs share game_sessions. Server routes in apps/server/src/mariokart.ts (adapted from smash.ts, FFA-only), pages in apps/web/src/mariokart/, own race-day red/blue theme (standing rule 3), own TV mode, materializes into matches/match_participants with the racer as `character` so it feeds the generic cross-game lifetime leaderboard (a dedicated character-stats endpoint like Smash's was skipped for now). Smash and Mario Kart deliberately don't share server/page code yet (kept Smash byte-stable since it's shipped and untestable locally); unifying them into one config-driven session pack is a future refactor. Quick play: POST /api/quickplay/{smash,mariokart} spins up a personal event.
- Async error boundary (2026-07-16): the crash-loop risk (a rejected promise in a route handler killing the process + WebSocket hub) is fixed without touching every handler and without a new dependency. apps/server/src/async-safe.ts monkeypatches the Express Router prototype's route-registration methods (get/post/use/…) to auto-wrap handlers so throws/rejections go to next(err); it is imported FIRST in index.ts so the patch is in place before any router registers. A top-level error middleware returns 500; process-level unhandledRejection/uncaughtException handlers are a last-resort net so anything outside a request handler (app-level middleware, ws callbacks, timers) still doesn't take the process down. 4-arg error handlers and mounted sub-routers (they carry a .stack) are left unwrapped so their arity/behaviour is unchanged. Verified in isolation: async reject → next(err), sync throw → next(err), wrapped arity stays 3, error handlers stay arity 4, sub-routers untouched.
- Arcade coverage complete (2026-07-16): the generic app is now fully Arcade. JoinPage, the App loading fallback, and the magic-link tap-through pages (auth.ts confirmPage/expiredPage, server-rendered so they pull the fonts in directly) were themed; the tournament recap/share card (recap.tsx) was redrawn in the Arcade palette (deep plum bg + coral glow, gold "GAMENIGHT OS" Luckiest Guy wordmark, gold champion, plum surfaces, teal accent tick, coral upset) and made format-aware (single vs double elim). The canvas waits on document.fonts before drawing so the webfonts (Luckiest Guy/Fredoka/Nunito) bake in instead of system-ui, with a fallback if they're unavailable. Branded packs (Beerio, Smash) stay independent by standing decision; the QR quiet-zone white stays white for scannability.
- Generic bracket readability (2026-07-16): the generic tournament view (BracketPage) and its TV view (TvPage) were redrawn. BracketPage now uses the SAME slot-height + connector layout the Beerio pack uses: each match is absolutely centered at (i+0.5)*slotH within its round column, slotH doubles per winners round (steps every second column in the losers bracket), so a later match lines up vertically between the two that feed it, with connector arms + vertical joiners tracing the path. Winners/Losers/Grand Final are labeled sections, each horizontally scrollable. Scoring is tap-a-name-to-win, tap-the-winner-again-to-undo (undo still confirms + cascades). The TV view stopped drawing the whole tree (unreadable across a room) and now mirrors Beerio's TV approach: big "On deck" (live matchups) + "Latest results" columns in vmin-scaled type, with the champion banner taking over when it's done. New CSS: gn-bkt-* and gn-tvm-* in index.css, reading only --gn-* tokens. BracketPage/TvPage are shared by BOTH the event flow and Quick Play (both route to /b/:id and /tv/:id), so there is no separate quick-play bracket — any bracket change lands in both by construction. Geometry was verified headlessly (card centers land exactly on feeder midpoints; columns tile without overlap).
- "Format" terminology (2026-07-16): the things you pick inside a pack (FFA, King of the Hill; Beerio's Bracket vs Grand Prix; single vs double elim) are called FORMATS in user-facing copy, not "game modes" ("modules" was considered and rejected as too software-y). Code-level type names (SmashMode etc.) were left alone; this is a copy decision. The Smash "End night" button became "End format" because completing a session returns the host to the format picker to start another run the same night.
- Double elim engine (2026-07-16): shipped in the shared engine as Session B. The declarative-graph pattern extended exactly as the seed app does it: a {t:"lose",m} slot source feeds losers-bracket matches, GF/GF2 are built up front with GF2 gated on GF going to slot B (the reset), and undo stays "delete a key + cascade" — downstreamOf() follows win AND lose edges plus an explicit GF→GF2 edge. Ids are stable per format (single elim keeps R{r}M{i}, so old brackets read fine; double uses W/L/GF ids). Placements moved into the engine (placements()): in double elim only losers-bracket and deciding-grand-final losses eliminate. No schema push needed: drizzle text enums are TypeScript-level, existing rows default single_elim. Format dropdowns: event bracket + Quick Play. Tests: packages/shared/tests/bracket.test.ts via `pnpm test:bracket` (rides the server package's tsx on purpose — adding a devDep without regenerating pnpm-lock.yaml would fail Render's frozen-lockfile install).
- Smash session UX polish (2026-07-16): FFA "Record a game" starts with every roster player checked (plus a check all / clear all toggle) since full-roster games are the norm; the KOTH TV view shows the next challenger and the rest of the line ("Up next" / "Then"), not just the current king.
- Smash leaderboard (2026-07-16): the crew stats page (/g/:id/stats) gains a Smash-specific panel on the Smash Bros tab, replacing the generic per-game list there. It surfaces everything the pack tracks: per-player wins/win-rate/main/variety/best-streak, per-fighter wins/win-rate, and member head-to-head. Head-to-head is derived from shared games (better finish wins the meeting; equal finish, e.g. two non-winners in a winner-only FFA, is a no-edge meeting). Best streak is the longest run of consecutive wins within a single night, ordered by game position, which reads as the KOTH king streak and as an FFA hot streak. All of it comes from the materialized ledger via /groups/:id/smash-stats; the generic aggregator stays untouched.
- Smash deploy schema miss (2026-07-16): the Session A deploy shipped code but the DB was missing match_participants.character and smash_sessions, so recording a game crash-looped the server on "column does not exist". Root cause: pnpm db:push:ci (drizzle-kit push --force) exited 0 without applying the additive change. push is introspection-diff based and its create/rename resolution expects a TTY; --force only bypasses the destructive-change confirmation, so in Render's non-interactive build it can no-op and still exit clean. Fix applied: idempotent ALTER/CREATE ... IF NOT EXISTS in the Neon console (no redeploy needed). Takeaway: for schema-changing deploys, confirm the build log shows "[✓] Changes applied", and if not, run the idempotent SQL directly. Additive CREATE/ALTER ... IF NOT EXISTS is always safe to run by hand.
- Smash scope split (2026-07-16): the pack ships as two sessions. Session A = FFA Night + King of the Hill + character system + TV + stats, with Tournament reusing the existing single-elim engine. Session B = double-elimination in the shared engine, surfaced as a format dropdown in both the generic Tournament tracker and the Smash Tournament option. Double-elim was split out because it touches non-Smash code and its undo (re-routing losers between brackets + grand-final reset) needs isolated testing.
- Smash storage model (2026-07-16): FFA and KOTH are session-based, not brackets. A night is a running log of individual games held in one server-side row per event (smash_sessions, jsonb state), so members join the host's session rather than a local copy (standing rule 2). Each completed game materializes into matches/match_participants immediately, keyed smash:{eventId}:{idx} for idempotent undo/redo. The jsonb is the live working state; the matches tables are the durable ledger.
- Smash character tracking (2026-07-16): added one nullable column, match_participants.character, so the fighter played survives into the lifetime ledger (otherwise "wins with <fighter>" is impossible). It is a generic per-participant label; brackets and Beerio leave it null. This is the only schema change in Session A besides the new smash_sessions table; both are additive and apply via the existing pnpm db:push:ci in the Render build.
- Smash roster (2026-07-16): one flat list of every fighter across all games (the complete Ultimate roster, static since Sora in Oct 2021), no per-game filtering. Echo fighters are separate entries and the three Mii Fighters are separate; Pokemon Trainer and Pyra/Mythra are single picks (tracking which Pokemon or twin is the kind of per-life granularity standing rule 9 says isn't worth the input). Pinned against a current source at build time, not from memory.
- Smash result detail (2026-07-16): winner-only is the default (one tap), with an opt-in full-placement toggle for FFA (1..N). Low barrier for entry, meticulous mode available. KOTH and Tournament are inherently winner/loser so the toggle only affects FFA.
- Smash character assignment (2026-07-16): host picks one mode per session: players self-select on their own devices (default), host taps once for random, or host-assigns everyone. Self-select writes are scoped so a member can only set their own slot; the host can set any.
- Smash stats endpoint (2026-07-16): character stats (wins by fighter, each member's main, variety) come from a dedicated /groups/:id/smash-stats endpoint reading the pack's ledger rows, leaving the generic /groups/:id/stats aggregator untouched. Keeps the character focus out of the cross-game leaderboard and lowers the risk of destabilizing a working endpoint (standing rule 9).
- UI direction (2026-07-15): Arcade chosen as the app's visual identity (deep plum, coral + teal "player" accents, chunky pressable buttons, Luckiest Guy wordmark). Tabletop was the runner-up and is saved as a future opt-in theme, not built now.
- Theming architecture (2026-07-15): the app moved off hardcoded Tailwind color utilities onto CSS custom-property tokens plus a small gn-* component-class layer in index.css. Reason: Tailwind color utilities compile to fixed values and cannot be re-themed at runtime; CSS variables can. A second theme (Tabletop) becomes another token block plus a switcher, with no component rewrites.
- Theme scope (2026-07-15): generic app screens get the Arcade theme; branded packs (Beerio Kart) keep their own styling by design. The generic tournament TV mode gets an Arcade big-screen variant; Beerio's TV mode is untouched.
- BACKLOG.md recovery (2026-07-15): the "Prepare for render" commit (94cc12b) accidentally overwrote BACKLOG.md with an old copy of the Beerio source. Restored from the last good copy (bef93b5) and brought current. If it ever happens again: `git checkout <good-commit> -- BACKLOG.md`.
- Hosting (resolved): deploys run on Render free tier with Postgres on NEON (Render's free DB is deleted after 30 days; Neon's is not). The Render build command MUST end with `pnpm db:push:ci` or deploys ship code without schema and crash-loop on missing columns. See RENDER.md.
- Beerio TV mode: reads the SAME public live-session endpoint the spectator view uses and renders with the SAME exported engine functions the host runs, so the big screen can never disagree with the phones. Polls every 3s (WebSocket hub is not wired into the beerio pack's own sync).
- Generic brackets materialize into matches/match_participants on completion. Undoing a result past completion RETRACTS the recorded rows, so the ledger always matches reality.
- Placement rule for generic brackets: champion 1st, then everyone ranked by how late they were eliminated. Guests are skipped (no identity to credit).
- Passwords added pre-v1 (revised from "magic links only"): Resend free-tier delivery limits made email-only login painful for testing and for friends pre-domain-verification. Signup does NOT verify email ownership; magic links remain the fallback and the only verified path. Revisit verification before any public launch.
- Bracket scoring defaults open to all members; group owners/admins can lock it per bracket.
- TV/spectator view is public read-only, keyed by the bracket UUID (unguessable link, like invite codes). No login on TVs. Revisit if brackets ever carry private data.
- Quick play (Option B, chosen over a parallel statless system): running a mode without a crew silently creates a PERSONAL crew (groups.is_personal, hidden from the crew list). One system: scoring, TV view, recap all work unchanged. Entrants can be typed guests; guests carry no lifetime stats until linked to a member.
- Bracket entrants are Entrant[] ({kind:"member",userId} | {kind:"guest",name}). Legacy rows holding bare userId strings are upgraded on read by parseEntrants(), so no data migration was needed.
- Leaving a crew: any member can leave; an owner cannot leave while others remain (no ownership transfer exists yet).
- Beerio divergence from the original (bug fix): champion celebration and Hall of Fame keys are seed-based, not name-based. Name-based keys re-fired the champion popup and wrote a HoF entry per keystroke when editing names on a completed bracket.
- Beerio prefill: launching from an event prefills setup with the yes-RSVP list ONLY when nothing is recorded yet; setup-screen names from an older saved night get replaced, an in-progress tournament never does. Fires once per event per device via a localStorage flag.
- Beerio stats binding: names match crew members case-insensitively at completion time; unmatched names are guests and unrecorded. A member renamed mid-night won't match; acceptable v1.
- Firefox back-button (bug fix): the auto event->s=CODE member bounce used push navigation, leaving a ghost history entry; switched to replace navigation, plus a "Connecting to the night" gate so members never see the host's setup screen mid-fetch.
- Recap card sharing (resolved, shipped): native share sheet first, download fallback.
- Mario Party tie placements (2026-07-18, bug fix): non-winners tied on stars now SHARE a placement (competition ranking: 1, 2, 2, 4) instead of getting an arbitrary order. The real in-game tiebreak is coins, which we deliberately don't track, so we don't invent one. The winner always holds 1st.
- Rivalry head-to-head definition (2026-07-18): an encounter is any completed match where both players have participant rows, across every pack; better (lower) placement wins it, equal placement ties. No "current streak" stat at the time: matches carried no timestamp, so encounter order could not be reconstructed. SUPERSEDED 2026-07-27 by matches.playedAt, which made streaks and recent form real; the rivalry compare itself still shows no streak, but both profiles do.
- Home "Your stats" aggregates across ALL crews (quick-play personal crew included, labeled "Quick play"); member profiles and rivalries stay crew-scoped because the ledger is group-scoped.

## OPEN DECISIONS
- Whether to reskin Beerio Kart to match Arcade later, or keep packs visually independent forever. Currently: packs stay independent.
