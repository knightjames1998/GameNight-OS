run = "pnpm start"
entrypoint = "apps/server/src/index.ts"

[nix]
channel = "stable-24_05"

[deployment]
build = ["sh", "-c", "npm install -g pnpm@9 && pnpm install && pnpm build"]
run = ["sh", "-c", "pnpm start"]

[[ports]]
localPort = 3000
externalPort = 80
